# Slice 03: parser-makefile-pr

> Ledger per `LEDGER-DISCIPLINE.md` (v2.0), Section A. All rows open at
> slice start, 2026-08-08. Closer: CC. Verifier: CDC. This slice absorbs
> upstream pandaPIparser PR #21 as the final selected upstream PR before
> release publication.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | Slice starts from `release/0.2.x` with Slice02 closed, CDC-verified, and the worktree clean. | `git branch --show-current`; `git status --short --branch`; `test -f docs/design-v0.2.0/arc02-upstream-absorption/slice02-engine-exit-status/closing-report.md`; `test -f docs/design-v0.2.0/arc02-upstream-absorption/slice02-engine-exit-status/cdc-verification.md` | serious | slice-doc section 1 | open | | |
| F-2 | Upstream parser PR #21 is refreshed at implementation time, with state, head SHA, author identity, merge/applicability status, and changed-file summary recorded. | `gh api repos/panda-planner-dev/pandaPIparser/pulls/21`; `gh api repos/panda-planner-dev/pandaPIparser/pulls/21/commits`; `git apply --check --directory=pandaPI/pandaPIparser <pr21.diff>` or reversible cherry-pick check | serious | arc-plan section 2 | open | | Slice-open snapshot: PR #21 open/non-draft, head `9735763ffd694d95c287766a447deba980d7deaa`, one Robert P. Goldman commit, one makefile touched, clean applicability. Refresh anyway. |
| F-3 | PR #21 is absorbed into `pandaPI/pandaPIparser/makefile` by native author-preserving cherry-pick where possible; any manual port is justified. | `git log --format=fuller <slice-start>..HEAD -- pandaPI/pandaPIparser/makefile`; inspect author and commit body/trailers | serious | project-plan P3; arc-plan A2 | open | | Native cherry-pick should be possible from the clean slice-open applicability check. |
| F-4 | The parser makefile change matches PR #21's bounded behavior: `all: pandaPIparser`, `pandaPIparser:` owns object dependencies, and `install: pandaPIparser` installs to `${DESTDIR}/bin`. | `git diff <slice-start>..HEAD -- pandaPI/pandaPIparser/makefile`; `rg -n '^all: pandaPIparser$|^pandaPIparser:|^install: pandaPIparser|install pandaPIparser' pandaPI/pandaPIparser/makefile` | correctness | PR #21 diff | open | | Do not broaden this into package-manager/install-layout design. |
| F-5 | Normal chengdu parser build still succeeds from in-tree source via the ignored build-source copy and produces `dist/<platform>/pandaPIparser`. | `./scripts/build-parser.sh`; `PLATFORM="$(bash -c '. scripts/lib-platform.sh; detect_platform')"`; `test -x "dist/$PLATFORM/pandaPIparser"`; inspect parser provenance block | serious | project DoD; build regression guard | open | | Use existing build scripts; do not build in tracked `pandaPI/`. |
| F-6 | The new parser `install` target works in a disposable destination without dirtying `pandaPI/`. | after `./scripts/build-parser.sh`: `PLATFORM="$(bash -c '. scripts/lib-platform.sh; detect_platform')"`; `tmp=$(mktemp -d)`; `mkdir -p "$tmp/bin"`; `make -C "build/$PLATFORM/source/pandaPIparser" DESTDIR="$tmp" install`; `test -x "$tmp/bin/pandaPIparser"`; `"$tmp/bin/pandaPIparser" --help`; `git status --short -- pandaPI` | serious | PR #21 title and implementation | open | | PR #21 assumes `${DESTDIR}/bin` exists; create it for this target smoke unless implementation intentionally adapts that behavior. |
| F-7 | Full local no-fetch source gate passes after the parser makefile change. | from clean generated output where practical: `./scripts/build-parser.sh`; `./scripts/build-grounder.sh`; `./scripts/build-engine.sh`; `./scripts/check-provenance.sh`; `./scripts/smoke-test.sh`; `./scripts/smoke-test.sh --negative` | serious | arc-plan A4/A5 | open | | If docs-close commit follows this gate, record that CDC should rebuild at close HEAD because provenance records the Git commit. |
| F-8 | Package dry-run preserves wolong's frozen release shape and validates checksums/manifests after the parser makefile change. | `./scripts/package-release.sh v0.2.0-slice03-smoke`; inspect `release/`; checksum verification used by package script | serious | project ledger P5 precursor | open | | Do not publish a release in this slice. |
| F-9 | Maintained shell/workflow/static checks pass and docs are diff-clean. | `bash -n scripts/*.sh`; `/bin/bash -n scripts/*.sh` where available; `shellcheck scripts/*.sh`; `actionlint .github/workflows/*.yml`; `git diff --check` | polish | local quality gate | open | | Shell changes are not expected. |
| F-10 | Non-selected upstream work remains outside the committed diff: engine PR #15, engine PR #9, grounder PR #7, grounder issue #8, and parser #15/#29 resource work stay no-op/deferred as triaged. | `git diff --name-only <slice-start>..HEAD`; inspect no changes under `pandaPI/pandaPIengine`, `pandaPI/pandaPIgrounder`, SAT verifier/vendor additions, wrapper scripts, Dockerfiles, parser resource-fix code paths | serious | arc-plan section 3 | open | | If live upstream state changes materially, record it as a release-slice/backlog input rather than absorbing it here. |
| F-11 | Upstream credit is present in implementation commits: original author identity is preserved or Robert P. Goldman is explicitly credited, and upstream PR URL is named. | `git log --format=fuller <slice-start>..HEAD -- pandaPI/pandaPIparser`; inspect commit body/trailers for `Upstream-PR: https://github.com/panda-planner-dev/pandaPIparser/pull/21` and required co-author trailers | serious | project ledger P3; arc ledger A2 | open | | Native author preservation is expected. |
| F-12 | Repo hygiene holds at close: worktree clean, no generated outputs under `pandaPI/`, no temporary PR worktree/branch/remotes left behind. | `git status --short --branch`; `find pandaPI -name .git -print`; `git ls-files -s pandaPI | awk '$1 == "160000"'`; `git worktree list`; `git remote -v` | correctness | arc01 no-gitlink/no-generated baseline | open | | The existing remote URL mismatch noted in Slice02 CDC is not this slice's responsibility unless CC changes remotes. |
| F-13 | Slice close report walks all 13 rows and includes Bubble-up to the arc/project: delivered-as-assigned, any install-target caveat, whether arc/project docs changed, and silent-drop diff. | `test -f docs/design-v0.2.0/arc02-upstream-absorption/slice03-parser-makefile-pr/closing-report.md`; inspect row count and Bubble-up section | serious | PROJECT-MANAGEMENT Part IV | open | | Do not write `cdc-verification.md`; CDC writes it after independent verification. |

## What Worked

_Fill during close._

## Follow-ups / Deferrals

_Fill during close._
