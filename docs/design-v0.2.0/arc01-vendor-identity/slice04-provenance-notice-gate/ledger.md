# Slice 04: provenance-notice-gate

> Ledger per `LEDGER-DISCIPLINE.md` (v2.0), Section A. All rows open at
> slice start, 2026-08-07. Closer: CC. Verifier: CDC. This is arc01's final
> slice; its close is the evidence base for the arc close.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | Slice starts from `release/0.2.x` with slice03 CDC-verified and worktree clean. | `git branch --show-current`; `git status --short`; `test -f docs/design-v0.2.0/arc01-vendor-identity/slice03-patch-dissolution/cdc-verification.md` | serious | arc-plan section 2 | open | | |
| F-2 | A committed `vendor.env` records the active vendored-source identity metadata for all seven history-imported source trees, and `pins.env` is explicitly historical rather than the active provenance checker source of truth. | `test -f vendor.env`; `. ./vendor.env`; `for v in PARSER GROUNDER ENGINE CPDDL H2_FD_PREPROCESSOR BORUVKA OPTS; do eval "test -n \"\\${${v}_PREFIX:-}\" && test -n \"\\${${v}_URL:-}\" && test -n \"\\${${v}_UPSTREAM_SHA:-}\" && test -n \"\\${${v}_IMPORT_COMMIT:-}\"" || exit 1; done`; inspect `pins.env` comments and active scripts | serious | project-plan section 3; arc-plan A6 | open | | |
| F-3 | Build provenance is re-anchored on chengdu source: each `dist/<platform>/provenance.txt` records the current chengdu commit, component source prefix, upstream URL/SHA as import identity, subtree import commit, `patches=none`, and compiler; it no longer uses a bare upstream `sha=` field as the build-source identity. | clean build; `cat dist/<platform>/provenance.txt`; `rg '^chengdu_commit=|^source_prefix=|^upstream_url=|^upstream_sha=|^import_commit=|^patches=none|^compiler=' dist/<platform>/provenance.txt`; `! rg '^sha=' dist/<platform>/provenance.txt` | serious | project-plan section 3; slice-doc section 2 | open | | |
| F-4 | `scripts/check-provenance.sh` validates the new provenance schema against `vendor.env` and git state, including current chengdu commit, import commit ancestry, and source-prefix existence; it fails a tampered provenance file. | `./scripts/check-provenance.sh`; copy `dist/<platform>/provenance.txt` to `/tmp`, edit one import/upstream/chengdu value, and verify `./scripts/check-provenance.sh --platform <platform> /tmp/<file>` exits nonzero | serious | project-plan P1/P2; arc-plan A2/A6 | open | | |
| F-5 | Build scripts append the new provenance fields for parser, grounder, and engine from `vendor.env`; none of the active build scripts source `pins.env` for current provenance. | inspect `scripts/build-parser.sh`, `scripts/build-grounder.sh`, `scripts/build-engine.sh`, `scripts/lib-platform.sh`; `rg 'pins\.env|PARSER_SHA|GROUNDER_SHA|ENGINE_SHA' scripts/build-parser.sh scripts/build-grounder.sh scripts/build-engine.sh scripts/check-provenance.sh` shows only historical/compat comments if any | correctness | project-plan section 3 | open | | |
| F-6 | `scripts/fetch-upstream.sh` is retired from active tooling and documentation, and no active script, workflow, README, or release note path tells users or CI to fetch planner source. | `test ! -f scripts/fetch-upstream.sh`; `! rg 'fetch-upstream\.sh|panda-planner-dev/.+git clone|gitlab.com/danfis/.+git clone|h2-fd-preprocessor.+git clone' README.md .github scripts docs/design-v0.2.0 .github/release-notes.md.in` | serious | project-plan section 3; arc-plan A3 | open | | If CC keeps historical source-inspection material, it must move outside active `scripts/` and be excluded from active user/CI paths. |
| F-7 | NOTICE/license delineation files exist for every vendored source surface named by slice-doc section 2, and each NOTICE names origin, import or derivation evidence, license, chengdu divergence policy, and arc01 modifications. | `for p in pandaPI/pandaPIparser pandaPI/pandaPIgrounder pandaPI/pandaPIengine pandaPI/pandaPIgrounder/cpddl pandaPI/pandaPIgrounder/h2-fd-preprocessor pandaPI/pandaPIgrounder/cpddl/third-party/boruvka pandaPI/pandaPIgrounder/cpddl/third-party/opts pandaPI/pandaPIgrounder/cpddl/third-party/bliss pandaPI/pandaPIgrounder/cpddl/third-party/lpsolve; do test -f "$p/NOTICE"; done`; `find pandaPI -name NOTICE -print | sort`; `find pandaPI -name NOTICE -print0 | xargs -0 rg 'Origin:|License:|Import|Derived|Divergence|Arc01'` | serious | arc-plan OQ4/A6 | open | | |
| F-8 | `docs/license-audit-v0.2.0.md` lands as an evidence-based audit revision for the in-tree fork baseline, distinguishing identity imports, slice03 patch-dissolution deltas, and future arc02 changes, and covering `bliss`, `boruvka`, `lpsolve`, and `opts` explicitly. | `test -f docs/license-audit-v0.2.0.md`; `rg 'identity import|patch-dissolution|arc02|bliss|boruvka|lpsolve|opts|h2-fd-preprocessor|GPL-3.0|BSD 3-Clause|LGPL' docs/license-audit-v0.2.0.md` | serious | project-plan P4; arc-plan OQ4/A6 | open | | |
| F-9 | `licenses/`, `licenses/README.md`, and `package-release.sh` are consistent with `docs/license-audit-v0.2.0.md`; generated `THIRD-PARTY-LICENSES` contains required license texts for the shipped binaries and no stale v0.1.0 audit authority. | `./scripts/package-release.sh v0.2.0-slice04-smoke`; `rg 'license-audit-v0.2.0|pandaPIparser|pandaPIgrounder|pandaPIengine|cpddl|boruvka|h2-fd-preprocessor' release/THIRD-PARTY-LICENSES licenses/README.md scripts/package-release.sh`; `! rg 'license-audit-v0.1.0' release/THIRD-PARTY-LICENSES licenses/README.md scripts/package-release.sh .github/release-notes.md.in README.md` | serious | project-plan P4; arc-plan A6 | open | | |
| F-10 | README and release-note template describe v0.2.0 source availability through chengdu's public repository at the release commit, not upstream repos plus `patches/`; release-note provenance table names chengdu/source/import identities instead of "Upstream SHA / Patches applied". | inspect `README.md` and `.github/release-notes.md.in`; `rg 'chengdu commit|source_prefix|upstream_sha|import_commit|vendor.env|license-audit-v0.2.0' README.md .github/release-notes.md.in`; `! rg 'pinned upstream sources|patches/|Upstream SHA \\| Patches applied|license-audit-v0.1.0' README.md .github/release-notes.md.in` | correctness | project-plan P4/P5; arc-plan A6 | open | | |
| F-11 | Local source-build behavior remains 0.1.0-equivalent after the provenance/licensing rewrite. | from clean `build/ dist/ release/`: `./scripts/build-parser.sh`; `./scripts/build-grounder.sh`; `./scripts/build-engine.sh`; `./scripts/check-provenance.sh`; `./scripts/smoke-test.sh`; `./scripts/smoke-test.sh --negative`; `./scripts/package-release.sh v0.2.0-slice04-smoke` | serious | arc-plan A4 | open | | |
| F-12 | Full GitHub Actions evidence exists for the two-platform no-fetch gate on this slice: Linux and macOS build legs, provenance check, positive/negative smoke, release packaging or equivalent artifact packaging, and log grep showing no planner-source clone/fetch. | record run URL(s); `gh run view <run> --json conclusion,headSha,event`; fetch relevant logs and `rg 'panda-planner-dev|gitlab.com/danfis|galvusdamor/h2-fd-preprocessor|fetch-upstream'` with only NOTICE/docs references allowed, no build-time clone/fetch lines | serious | project-plan P2; arc-plan A3/A4 | open | | If GitHub Actions is unavailable, this row is not done; record blocked/deferred with a concrete re-entry condition. |
| F-13 | The release manifest and rendered notes generated by packaging carry the new provenance/license story and preserve wolong's frozen asset shape. | after package dry-run, inspect `release/release-manifest.txt`, `release/notes.md`, `release/SHA256SUMS`; `for t in release/pandapi-v0.2.0-slice04-smoke-*.tar.gz; do tar tzf "$t" | sort; done`; verify tarball contents remain `fixtures/`, `pandaPIparser`, `pandaPIgrounder`, `pandaPIengine`, `provenance.txt` | serious | project-plan P5; arc-plan A4/A6 | open | | |
| F-14 | Maintained scripts/workflows/docs stay lintable and the vendored source tree remains clean after builds. | `bash -n scripts/*.sh`; `shellcheck scripts/*.sh`; `actionlint .github/workflows/*.yml`; `git diff --check`; `git status --short -- pandaPI`; `find pandaPI -type f \\( -name '*.o' -o -name '*.a' -o -name pandaPIparser -o -name pandaPIgrounder -o -name pandaPIengine \\) -print` returns empty | polish | local quality gate; slice02 invariant | open | | |

## What Worked

<!-- Filled by CC during close. -->
