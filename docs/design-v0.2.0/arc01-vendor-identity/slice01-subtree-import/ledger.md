# Slice 01: subtree-import

> Ledger per `LEDGER-DISCIPLINE.md` (v2.0), Section A. All rows open at
> slice start, 2026-08-07. Closer: CC. Verifier: CDC. This slice is an
> identity/history import slice only; build repointing starts in slice02.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | The slice starts from a clean worktree on `release/0.2.x`, and no `pandaPI/` tree already exists. | `git branch --show-current`; `git status --short`; `test ! -e pandaPI` | serious | slice-doc section 3 | done | Commit `0474f383c61815a35a94f0413ac6b6a72ec7feaa` (slice start). `git branch --show-current` -> `release/0.2.x`; `git status --short` -> empty; `test ! -e pandaPI` -> exit 0. Strength: attested. | |
| F-2 | `pandaPI/pandaPIparser` is imported with full history, no squash, at `88c0995c72c5ce2685e9546ec153a3545977ed81`. | `git merge-base --is-ancestor 88c0995c72c5ce2685e9546ec153a3545977ed81 HEAD`; normalized `git ls-tree` diff against parser SHA is empty | serious | project-plan section 3; arc-plan section 3 | done | Source `https://github.com/panda-planner-dev/pandaPIparser.git`; SHA `88c0995c72c5ce2685e9546ec153a3545977ed81`; import commit `ac8e385d`. Verify output: `PASS ancestor parser ...`; `PASS tree parser diff empty (/tmp/chengdu-parser.tree.diff)`. Strength: attested. | Record the subtree import commit. |
| F-3 | `pandaPI/pandaPIengine` is imported with full history, no squash, at `810f04388667db5e3e4f114e960a4efbb43b1ac0`. | `git merge-base --is-ancestor 810f04388667db5e3e4f114e960a4efbb43b1ac0 HEAD`; normalized `git ls-tree` diff against engine SHA is empty | serious | project-plan section 3; arc-plan section 3 | done | Source `https://github.com/panda-planner-dev/pandaPIengine.git`; SHA `810f04388667db5e3e4f114e960a4efbb43b1ac0`; import commit `f734b692`. Verify output: `PASS ancestor engine ...`; `PASS tree engine diff empty (/tmp/chengdu-engine.tree.diff)`. Strength: attested. | Record the subtree import commit. |
| F-4 | `pandaPI/pandaPIgrounder` is imported with full history, no squash, at `b04ff7bcca710ae80a1ce73b8c027f0224565afc`, and its import commit matches upstream before de-submoduling. | `git merge-base --is-ancestor b04ff7bcca710ae80a1ce73b8c027f0224565afc HEAD`; normalized tree diff against grounder SHA is empty at the grounder import commit | serious | project-plan section 3; arc-plan section 3; arc-plan OQ1 | done | Source `https://github.com/panda-planner-dev/pandaPIgrounder.git`; SHA `b04ff7bcca710ae80a1ce73b8c027f0224565afc`; import commit `2b0a6093`. Verify output: `PASS ancestor grounder ...`; `PASS tree grounder-import diff empty (/tmp/chengdu-grounder-import.tree.diff)`. Strength: attested. | The comparison at this stage still sees gitlinks for the former submodules. Final parent tree excluding replaced `cpddl` and `h2-fd-preprocessor` also passed: `/tmp/chengdu-grounder-final-parent.tree.diff`. |
| F-5 | The imported grounder tree records gitlinks for `cpddl` and `h2-fd-preprocessor` at the expected SHAs before those paths are replaced. | `git ls-tree <grounder-import-commit>:pandaPI/pandaPIgrounder cpddl h2-fd-preprocessor` shows `160000 commit 8c142bab211200213746c75be71810526e834d4a` and `160000 commit e335433bfc53171a1c215bd49ba39cc5fb1d485b` | correctness | arc-plan OQ1 | done | At `2b0a6093:pandaPI/pandaPIgrounder`, `git ls-tree ... cpddl h2-fd-preprocessor` returned `160000 commit 8c142bab211200213746c75be71810526e834d4a cpddl` and `160000 commit e335433bfc53171a1c215bd49ba39cc5fb1d485b h2-fd-preprocessor`. Strength: attested. | This is the bridge between upstream grounder identity and final in-tree source layout. |
| F-6 | `pandaPI/pandaPIgrounder/cpddl` is imported with full history, no squash, at `8c142bab211200213746c75be71810526e834d4a`, replacing the grounder gitlink at the same path. | `test -d pandaPI/pandaPIgrounder/cpddl/src`; `git merge-base --is-ancestor 8c142bab211200213746c75be71810526e834d4a HEAD`; normalized tree diff against cpddl SHA is empty | serious | project-plan section 3; arc-plan section 3 | done | Source `https://gitlab.com/danfis/cpddl.git`; SHA `8c142bab211200213746c75be71810526e834d4a`; import commit `25a278a5`; grounder gitlink removal commit `8d24ece8`. Verify output: `test -d .../cpddl/src` exit 0; `PASS ancestor cpddl ...`; `PASS tree cpddl-import diff empty (/tmp/chengdu-cpddl-import.tree.diff)`. Strength: attested. | The cpddl import commit matches upstream with its own gitlinks; final parent tree excluding replaced `third-party/boruvka` and `third-party/opts` also passed: `/tmp/chengdu-cpddl-final-parent.tree.diff`. |
| F-7 | `pandaPI/pandaPIgrounder/h2-fd-preprocessor` is imported with full history, no squash, at `e335433bfc53171a1c215bd49ba39cc5fb1d485b`, replacing the grounder gitlink at the same path. | `test -d pandaPI/pandaPIgrounder/h2-fd-preprocessor/src`; `git merge-base --is-ancestor e335433bfc53171a1c215bd49ba39cc5fb1d485b HEAD`; normalized tree diff against h2 SHA is empty | serious | project-plan section 3; arc-plan section 3 | done | Source `https://github.com/galvusdamor/h2-fd-preprocessor.git`; SHA `e335433bfc53171a1c215bd49ba39cc5fb1d485b`; import commit `b27e102c`; grounder gitlink removal commit `8d24ece8`. Verify output: `test -d .../h2-fd-preprocessor/src` exit 0; `PASS ancestor h2-fd-preprocessor ...`; `PASS tree h2-fd-preprocessor diff empty (/tmp/chengdu-h2-fd-preprocessor.tree.diff)`. Strength: attested. | Use the grounder's pinned submodule SHA and `.gitmodules` URL as the identity source. |
| F-8 | The final `pandaPI/` layout has real source directories, not gitlinks or nested `.git` repositories, at every planned prefix. | `find pandaPI -name .git -print` returns nothing; `git ls-files -s pandaPI | awk '$1 == "160000" { print }'` returns nothing | correctness | slice-doc section 2 | done | Verified at `68072cb3`: `find pandaPI -name .git -print` -> empty; `git ls-files -s pandaPI ...160000...` -> empty. Additional cpddl gitlinks discovered and replaced: `boruvka` source `https://gitlab.com/danfis/boruvka.git`, SHA `e320cc7b02714e4096a686965fab343735d82bec`, import `1f0ac094`; `opts` source `https://github.com/danfis/opts.git`, SHA `819d90e8664f9deda56f0dd8e7b62ea3ab9b2f6d`, import `68072cb3`. Strength: attested. | Full history is in chengdu history, not nested repos. The extra cpddl third-party gitlinks are disclosed in the closing report bubble-up. |
| F-9 | Existing 0.1.0 build/release inputs are not behaviorally changed in this slice: scripts, workflows, `pins.env`, `patches/`, fixtures, and release packaging remain untouched except for unavoidable git metadata/history. | `git diff --name-only <slice-start>..HEAD -- scripts .github pins.env patches fixtures README.md licenses docs/license-audit-v0.1.0.md` returns nothing | correctness | slice-doc section 2 out-list | done | Verified at `68072cb3`: `git diff --name-only 0474f383c61815a35a94f0413ac6b6a72ec7feaa..HEAD -- scripts .github pins.env patches fixtures README.md licenses docs/license-audit-v0.1.0.md` -> empty. Strength: attested. | No build/release inputs were changed. |
| F-10 | Import evidence is complete enough for CDC reproduction: source URL, SHA, import commit, and tree-identity command/output are recorded for all five imported components. | Inspect this ledger evidence and closing report; count 5 component evidence blocks | serious | LEDGER-DISCIPLINE.md Section A | done | Rows F-2 through F-7 record the five assigned component URLs, SHAs, import commits, and tree checks. Row F-8 and the closing report also record the two unanticipated cpddl nested subtree imports needed to satisfy the no-gitlinks invariant. Strength: attested. | Evidence can be attached in ledger rows or the closing report, but every row must be reproducible. |
| F-11 | No unplanned files are introduced outside `pandaPI/` and this slice's own open/close artifacts. | `git diff --name-only <slice-start>..HEAD` reviewed against slice-doc section 2 | polish | slice-doc section 3 | done | Reviewed committed diff from `0474f383c61815a35a94f0413ac6b6a72ec7feaa..68072cb3` plus `git status --short`: changes are under `pandaPI/` plus this slice's `ledger.md` and `closing-report.md`. Strength: attested. | The close-set file `closing-report.md` is expected at slice close. The cpddl nested dependencies are under `pandaPI/` and were necessary to satisfy F-8. |

## What Worked

- Checking for all `160000` entries across `pandaPI/` immediately after the assigned imports caught cpddl's nested `boruvka` and `opts` gitlinks before close.
- Keeping the first import commits intact made parent-tree identity checks straightforward: compare the raw import commit for upstream identity, then compare the final parent tree with only the intentionally replaced submodule paths filtered.

## Closure

Closed by CC on 2026-08-07. Verified by: pending CDC.
Rows: 11. Done: 11. Deferred: 0. No-op: 0.
