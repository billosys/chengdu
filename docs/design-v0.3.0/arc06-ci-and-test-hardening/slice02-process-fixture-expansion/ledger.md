# Arc06 Slice02: process-fixture-expansion

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | Make exposes a managed pipeline contract target and lists it in `make help`. | `set -e; make help | rg -n "test-contract-pipeline-managed"; make test-contract-list-managed | rg -n "pipeline"` | serious | Slice01 inventory; Make entrypoint policy | open | | |
| F-2 | The contract harness can list and select managed pipeline fixtures through `--contract --component pipeline` without direct CI use of the harness. | `set -e; ./tests/contract/run --contract --component pipeline --list | rg -n "pipeline"` | correctness-grade | fixture harness capability | open | | Direct harness use is acceptable here as a harness capability check; CI/handoffs use Make. |
| F-3 | Fixture records document the new managed pipeline and composition cases, including argv, artifact, stdout/stderr, exit/status, and comparison policy. | `set -e; for term in "pipeline" "parse-ground-solve" "pairwise" "artifact" "stdout" "stderr" "PANDAPI_STATUS" "comparison"; do rg -n -- "$term" fixtures/contract docs/design-v0.3.0/arc06-ci-and-test-hardening >/dev/null; done` | correctness-grade | Arc03 matrix; Slice01 inventory | open | | |
| F-4 | A managed file-backed parse-ground-solve pipeline fixture passes using canonical `pandapi-*` commands and final status on stderr at each step. | `make test-contract-pipeline-managed` | serious | Slice01 recommended first batch | open | | |
| F-5 | Pairwise artifact-composition fixtures prove parser stdout output can feed grounder input after capture, and grounder stdout output can feed engine input after capture, without inventing stdin support. | `set -e; make test-contract-pipeline-managed; rg -n -- "parser stdout" docs/design-v0.3.0/arc06-ci-and-test-hardening fixtures/contract >/dev/null; rg -n -- "grounder stdout" docs/design-v0.3.0/arc06-ci-and-test-hardening fixtures/contract >/dev/null` | serious | Slice01 gap map | open | | |
| F-6 | Positive `--supervised` fixtures pass for parser, grounder, and engine with no ANSI leakage and stable stdout/stderr ownership. | `set -e; make test-contract-parser-managed; make test-contract-grounder-managed; make test-contract-engine-managed; for term in "supervised" "ANSI-free" "stdout" "stderr"; do rg -n -- "$term" fixtures/contract tests/contract/run docs/design-v0.3.0/arc06-ci-and-test-hardening >/dev/null; done` | serious | Arc03 supervised-process target | open | | |
| F-7 | Deterministic output-finalization failure coverage is added where portable, or explicitly deferred with rationale and re-entry condition. | `set -e; rg -n -- "output-finalization|finalization failure|flush|Re-entry" docs/design-v0.3.0/arc06-ci-and-test-hardening/slice02-process-fixture-expansion docs/design-v0.3.0/arc06-ci-and-test-hardening/fixture-gap-inventory.md >/dev/null` | correctness-grade | Slice01 gap map | open | | |
| F-8 | The fixture gap inventory is updated so coverage/sanitizer slices know which expanded workload is available and which gaps remain. | `set -e; for term in "Slice02" "managed pipeline" "representative" "coverage" "sanitizer"; do rg -n -- "$term" docs/design-v0.3.0/arc06-ci-and-test-hardening/fixture-gap-inventory.md >/dev/null; done` | correctness-grade | bubble-up discipline | open | | |
| F-9 | The slice does not promote inherited optional surfaces or reintroduce inherited `pandaPI*` command compatibility as accepted behavior. | `set -e; ! git diff --cached -U0 -- tests fixtures mk docs/design-v0.3.0/arc06-ci-and-test-hardening | rg -n "pandaPIparser|pandaPIgrounder|pandaPIengine|legacy_binary|compatibility accepted"` | serious | Arc05 no-compatibility policy | open | | Historical/no-op text must be carefully scoped if touched. |
| F-10 | No release package, publish, checksum, manifest, wolong, README, Arc07, or Arc08 surface is changed. | `set -e; ! git diff --cached --name-only -- release tools/release .github/workflows/release.yml README.md docs/design-v0.3.0/arc07-pandapi-tutorial-docs docs/design-v0.3.0/arc08-release-prep-publication | rg .` | serious | Arc06 boundary | open | | |
| F-11 | New CI or workflow behavior, if any, calls Make targets rather than project scripts. | `make safety-checks && make actionlint` | serious | CI entrypoint policy | open | | |
| F-12 | Existing quality and behavior gates still pass after fixture expansion. | `make static-analysis && make format-check && make test && make provenance-check && git diff --check && git diff --cached --check` | serious | no regression | open | | |

## What Worked

_(Fill during close.)_
