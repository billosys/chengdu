# Arc07 Slice03 Ledger: first-party-source-naming-normalization

| Row | Criterion | Verify command | Strength | Parent | Status | Evidence | Notes |
|-----|-----------|----------------|----------|--------|--------|----------|-------|
| F-1 | Make exposes and documents canonical source naming report/check targets for the first-party source-quality surface. | `set -e; make help \| rg "source-quality-naming-(report\|check)"; rg -n -- "source-quality-naming-(report\|check)" Makefile mk tools >/dev/null` | serious | Arc07 A1/A3/A7 | done | `make help \| rg "source-quality-naming-(report\|check)"` passes; `rg -n -- "source-quality-naming-(report\|check)" Makefile mk tools` finds the Make targets, help text, and helper subcommands. | Targets are Make-backed through `mk/source-quality.mk`; no new `scripts/` entrypoint. |
| F-2 | A durable Arc07 source naming report records the accepted lower snake case policy, source-class exclusions, and final rename map. | `set -e; test -s docs/design-v0.3.0/arc07-source-quality-expansion/source-naming-normalization.md; for term in "lower snake case" "first-party maintained" "rename map" "excluded" "cpddl" "h2-fd-preprocessor" "cudd-3.0.0"; do rg -n -- "$term" docs/design-v0.3.0/arc07-source-quality-expansion/source-naming-normalization.md >/dev/null; done` | correctness-grade | Arc07 A1/A2/A5 | done | `make source-quality-naming-report` writes `docs/design-v0.3.0/arc07-source-quality-expansion/source-naming-normalization.md`; the required term check passes. | The report distinguishes policy, generated/build/dependency exclusions, and actual renames. |
| F-3 | Non-conforming first-party maintained C/C++ file and directory names are renamed with `git mv`, preserving history and avoiding macOS case-only rename hazards. | `set -e; git diff --cached --summary \| rg "rename .*pandaPI/(parser\|grounder\|engine\|runtime)" >/dev/null; ! git diff --cached --summary \| rg "delete mode|create mode" \| rg "pandaPI/(parser\|grounder\|engine\|runtime)"` | serious | Arc07 A7 | done | First-party parser, grounder, and engine path changes were made with `git mv`; staged summary check passes after staging. | No source files are intentionally added or removed as plain create/delete pairs. |
| F-4 | The Make-backed naming check passes and proves first-party maintained C/C++ file and directory basenames conform to the accepted lower snake case policy. | `make source-quality-naming-check` | correctness-grade | Arc07 A3/A7 | done | `make source-quality-naming-check` passes and reports `Source-quality naming check passed for macos-arm64`. | Conventional build filenames such as `CMakeLists.txt`, `Makefile`, and `makefile` are documented exemptions. |
| F-5 | Parser path renames are complete: includes/build references are updated, parser builds, and managed parser contract fixtures still pass. | `make build-parser && make test-contract-parser-managed` | serious | Arc07 A3/A7 | done | `make build-parser` passes; `make test-contract-parser-managed` passes with `SUMMARY: 298 passed, 0 failed`. | Parser changes are path/include/build reference updates only. |
| F-6 | Grounder path renames are complete: includes/build references are updated, grounder builds, and managed grounder contract fixtures still pass. | `make build-grounder && make test-contract-grounder-managed` | serious | Arc07 A3/A7 | done | `make build-grounder` passes; `make test-contract-grounder-managed` passes with `SUMMARY: 269 passed, 0 failed`. | `cpddl` and `h2-fd-preprocessor` remain excluded and untouched. |
| F-7 | Engine path renames are complete: includes/CMake references are updated, engine builds, and managed engine contract fixtures still pass. | `make build-engine && make test-contract-engine-managed` | serious | Arc07 A3/A7 | done | `make build-engine` passes; `make test-contract-engine-managed` passes with `SUMMARY: 312 passed, 0 failed`. | CUDD remains excluded; optional/fenced source behavior is not promoted. |
| F-8 | Source-class exclusions remain intact: generated output, copied build artifacts, vendored third-party source, dependency-internal source, and third-party generated code are not silently promoted or renamed. | `set -e; ! git diff --cached --name-only -- pandaPI/grounder/cpddl pandaPI/grounder/h2-fd-preprocessor pandaPI/engine/src/symbolic_search/cudd-3.0.0 \| rg .; make source-quality-surface; for term in "GENERATED" "THIRD_PARTY" "DEPENDENCY" "COPIED_BUILD" "BUILD_OUTPUT"; do rg -n -- "$term" build/source-quality/*/source-quality-surface.md >/dev/null; done` | serious | Arc07 A4/A5/A6 | done | Boundary command passes after staging; `make source-quality-surface` continues to write source-class evidence with required exclusion classes. | No excluded path changed. |
| F-9 | Slice02 source-quality surfaces still work after renaming and produce compile database/profile evidence with the new canonical paths. | `make source-quality-surface && make source-quality-profile-map && make compile-db-first-party` | serious | Arc07 A1/A3/A7 | done | `make source-quality-surface`, `make source-quality-profile-map`, and `make compile-db-first-party` pass and regenerate reports under `build/source-quality/macos-arm64/`. | Compile database/profile evidence is generated against canonical renamed paths. |
| F-10 | Existing Make-backed build, test, static-analysis, format, safety, and provenance gates still pass, or any skipped `make check` equivalent is explained precisely. | `make safety-checks && make actionlint && make static-analysis && make format-check && make build && make test && make provenance-check && git diff --check && git diff --cached --check` | serious | no regression | done | Required constituent gates pass; see closing report Verification. | `make check` was not run separately; the reproduced narrower gates include build, test, provenance, static analysis, format, safety, actionlint, and whitespace checks. |
| F-11 | The slice does not expand public behavior, CI workflows, release packaging, README/tutorial docs, Arc08, Arc09, or quality thresholds. | `set -e; ! git diff --cached --name-only -- .github README.md release tools/release docs/design-v0.3.0/arc08-pandapi-tutorial-docs docs/design-v0.3.0/arc09-release-prep-publication \| rg .; ! git diff --cached -G 'coverage floor|release blocker coverage|fail.*warning|fail.*coverage|supported public|wolong|GitHub Actions' -- Makefile mk tools \| rg .` | serious | boundary | done | Boundary command passes after staging. | Public docs can receive this policy later in Arc08, not here. |
| F-12 | Closing report walks every row and includes Bubble-up to Arc07, explicitly stating whether Slice04 first-party-quality-gate-scaffold should open next or whether naming work revealed a required split/remediation. | `set -e; rg -n -- "F-1|F-2|F-3|F-4|F-5|F-6|F-7|F-8|F-9|F-10|F-11|F-12|Bubble-up to Arc07|Slice04|remediation|split" docs/design-v0.3.0/arc07-source-quality-expansion/slice03-first-party-source-naming-normalization/closing-report.md` | correctness-grade | project-management | done | Closing report added and grep command passes. | Do not create `cdc-verification.md`; CDC writes it after independent review. |

## Ledger Notes

- The naming policy is scoped to first-party maintained C/C++ source and source
  directories. It does not rename vendored third-party/dependency-internal
  paths or generated/build outputs.
- The check target should be deterministic and suitable for later inclusion in
  first-party source-quality gates.
- Use `git mv` for renames. On macOS, case-only renames should use a temporary
  intermediate name when required so Git records the rename correctly.
- If the rename set is too large to fit one context with verification
  headroom, stop and request a split rather than partially normalizing a
  component and calling the row done.
