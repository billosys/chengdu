# Slice 07: grounder-native-contract-cutover

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | The slice open set records grounder-only native cutover scope, Slice05/Slice06 context, parser-native preservation, engine deferral, no Bash adapter, no grounder old-name artifacts, make-targeted verification, and release-publication boundary. | `set -e; test -f docs/design-v0.3.0/arc05-binary-contract-adoption/slice07-grounder-native-contract-cutover/slice-doc.md; test -f docs/design-v0.3.0/arc05-binary-contract-adoption/slice07-grounder-native-contract-cutover/cc-prompt.md; for term in "grounder-only" "Slice05" "Slice06" "pandapi-grounder" "Bash adapter" "pandaPIgrounder.legacy" "Parser native" "Engine remains" "make" "No release publication"; do rg -n -- "$term" docs/design-v0.3.0/arc05-binary-contract-adoption/slice07-grounder-native-contract-cutover/slice-doc.md docs/design-v0.3.0/arc05-binary-contract-adoption/slice07-grounder-native-contract-cutover/cc-prompt.md >/dev/null; done` | serious | Slice06 bubble-up | open | | |
| F-2 | Grounder build output installs executable `pandapi-grounder` and does not install `pandaPIgrounder` or `pandaPIgrounder.legacy` in `dist/<platform>/`. | `set -e; make build-grounder; . tools/shared/platform; platform="$(detect_platform)"; test -x "dist/$platform/pandapi-grounder"; ! test -e "dist/$platform/pandaPIgrounder"; ! test -e "dist/$platform/pandaPIgrounder.legacy"` | serious | no inherited grounder compatibility | open | | |
| F-3 | `dist/<platform>/pandapi-grounder` is not a shell script or copied Bash adapter. | `set -e; make build-grounder; . tools/shared/platform; platform="$(detect_platform)"; file "dist/$platform/pandapi-grounder" | rg -v 'shell script|text executable|ASCII text|UTF-8 text' >/dev/null; if head -c 2 "dist/$platform/pandapi-grounder" 2>/dev/null | rg -q '^#!'; then exit 1; fi` | serious | Slice05 stop condition | open | | |
| F-4 | Grounder managed-process options are handled natively by `pandapi-grounder`, including `--status=stderr`, `--status=stdout`, `--supervised`, `--output`, `--help`, `--version`, and `--provenance`. | `set -e; make test-contract-grounder-managed; ./tests/contract/run --contract --component grounder --case grounder-info-commands; ./tests/contract/run --contract --component grounder --case grounder-status-stream-legality; ./tests/contract/run --contract --component grounder --case grounder-final-status-fields` | serious | Arc03 contract | open | | |
| F-5 | H2 and `cpddl`/FAM grounder surfaces remain fenced with managed status instead of being promoted to supported normal grounding behavior. | `set -e; make test-contract-grounder-managed; ./tests/contract/run --contract --component grounder --case grounder-h2-surface-fencing; ./tests/contract/run --contract --component grounder --case grounder-cpddl-fam-surface-fencing` | serious | Arc03 supported surface | open | | |
| F-6 | Active build/test/tooling surfaces do not invoke or install `scripts/pandapi-grounder-adapter.sh`, `scripts/install-grounder-adapter.sh`, `pandaPIgrounder`, or `pandaPIgrounder.legacy` as grounder artifacts. | `set -e; ! rg -n -- 'pandapi-grounder-adapter|install-grounder-adapter|pandaPIgrounder\\.legacy|cp .*pandaPIgrounder|pandaPIgrounder remains executable|legacy_binary=pandaPIgrounder' Makefile mk tests tools fixtures/contract README.md` | serious | grounder shim retirement | open | | Source paths under `pandaPI/pandaPIgrounder` remain allowed. |
| F-7 | Grounder transition shim scripts are deleted and the parser transition shim remains absent. | `set -e; ! test -e scripts/pandapi-grounder-adapter.sh; ! test -e scripts/install-grounder-adapter.sh; ! test -e scripts/pandapi-parser-adapter.sh` | serious | grounder shim retirement | open | | |
| F-8 | Grounder fixture records no longer assert inherited grounder command compatibility, grounder `.legacy` sidecars, or grounder adapter provenance. | `set -e; rg -n -- 'pandapi-grounder' fixtures/contract/grounder-contract-records.md >/dev/null; ! rg -n -- 'pandaPIgrounder|pandaPIgrounder\\.legacy|pandapi-grounder-adapter|install-grounder-adapter|legacy_binary=pandaPIgrounder|remains executable' fixtures/contract/grounder-contract-records.md fixtures/contract/README.md` | serious | executable contract records | open | | |
| F-9 | Grounder provenance/version output names `pandapi-grounder` as the canonical command, does not advertise a grounder `legacy_binary` sidecar, and still records H2 and `cpddl`/FAM dependency-surface disposition. | `set -e; make build-grounder; . tools/shared/platform; platform="$(detect_platform)"; "dist/$platform/pandapi-grounder" --provenance > /tmp/pandapi-grounder-provenance.txt; rg -n -- 'canonical_command=pandapi-grounder|component=grounder|managed_process_contract=0.3.0|dependency_surface_h2=experimental_surface|dependency_surface_cpddl_fam=legacy_surface' /tmp/pandapi-grounder-provenance.txt >/dev/null; ! rg -n -- 'legacy_binary=pandaPIgrounder|invoked_command=pandaPIgrounder' /tmp/pandapi-grounder-provenance.txt` | serious | provenance contract | open | | |
| F-10 | Parser native behavior remains preserved after grounder cutover: parser contract passes and parser old-name artifacts remain absent. | `set -e; make test-contract-parser-managed; . tools/shared/platform; platform="$(detect_platform)"; test -x "dist/$platform/pandapi-parser"; ! test -e "dist/$platform/pandaPIparser"; ! test -e "dist/$platform/pandaPIparser.legacy"` | serious | Slice06 regression proof | open | | |
| F-11 | Grounder managed contract fixtures and smoke coverage pass through make after the native cutover. | `set -e; make test-contract-grounder-managed; make smoke; make smoke-negative` | serious | behavior preservation | open | | |
| F-12 | `make test` passes with parser and grounder native and engine still inherited. | `make test` | serious | release-branch quality gate | open | | |
| F-13 | The implementation does not modify parser source except necessary regression fallout, engine source, release publication workflow, checksums, final release manifests, Arc05 close files, or CDC files. | `set -e; test -z "$({ git diff --name-only -- pandaPI/pandaPIengine .github/workflows/release.yml release-manifest.txt SHA256SUMS docs/design-v0.3.0/arc05-binary-contract-adoption/closing-report.md docs/design-v0.3.0/arc05-binary-contract-adoption/slice07-grounder-native-contract-cutover/cdc-verification.md; git diff --cached --name-only -- pandaPI/pandaPIengine .github/workflows/release.yml release-manifest.txt SHA256SUMS docs/design-v0.3.0/arc05-binary-contract-adoption/closing-report.md docs/design-v0.3.0/arc05-binary-contract-adoption/slice07-grounder-native-contract-cutover/cdc-verification.md; } | tr -d '\n')"` | serious | protected boundary | open | | |
| F-14 | The implementation adds no default-path network fetch, vendored package, held/rejected dependency import, or third-party API leakage into planner internals. | `set -e; ! { { git diff -U0 -- pandaPI/pandaPIgrounder pandapi-runtime fixtures mk tests tools scripts .github README.md; git diff --cached -U0 -- pandaPI/pandaPIgrounder pandapi-runtime fixtures mk tests tools scripts .github README.md; } | rg -n -e "FetchContent" -e "ExternalProject" -e "curl" -e "git clone" -e "find_package\\(nlohmann" -e "find_package\\(Boost" -e "find_package\\(absl" -e "find_package\\(GSL" -e "#include <nlohmann" -e "boost/" -e "absl/" -e "gsl/"; }` | serious | Arc02 dependency gates | open | | |
| F-15 | Staged and unstaged whitespace checks pass. | `set -e; git diff --check; git diff --cached --check` | serious | repository hygiene | open | | |
| F-16 | CC writes a closing report with capability verdict, implementation decisions, verification, ledger walk, silent-drop check, and Bubble-up to Arc05. | `set -e; test -f docs/design-v0.3.0/arc05-binary-contract-adoption/slice07-grounder-native-contract-cutover/closing-report.md; for term in "Capability Verdict" "Implementation Decisions" Verification "Ledger Walk" "Silent-Drop Check" "Bubble-up to Arc05"; do rg -n -- "$term" docs/design-v0.3.0/arc05-binary-contract-adoption/slice07-grounder-native-contract-cutover/closing-report.md >/dev/null; done` | serious | ledger discipline | open | | |

## Notes

- This is the grounder half of the Slice05 remediation. Do not change engine
  adoption here.
- Parser native behavior from Slice06 is a protected regression surface.
- New GitHub Actions / CI jobs must invoke make targets, not project scripts.
