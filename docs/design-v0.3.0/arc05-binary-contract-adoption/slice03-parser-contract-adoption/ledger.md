# Slice 03: parser-contract-adoption

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | The slice open set documents parser-only contract adoption scope, canonical/inherited command proof, parser positive/negative fixtures, stream/status/artifact requirements, CI, and downstream grounder handoff. | `set -e; test -f docs/design-v0.3.0/arc05-binary-contract-adoption/slice03-parser-contract-adoption/slice-doc.md; for term in parser-only pandapi-parser pandaPIparser "PANDAPI_STATUS" stdout stderr artifact CI grounder; do rg -n -- "$term" docs/design-v0.3.0/arc05-binary-contract-adoption/slice03-parser-contract-adoption/slice-doc.md >/dev/null; done` | serious | Arc05 slice03 planning | open | | |
| F-2 | Parser build output provides both canonical `pandapi-parser` and inherited `pandaPIparser` executables in `dist/<platform>/`. | `set -e; ./scripts/build-parser.sh; . scripts/lib-platform.sh; platform="$(detect_platform)"; test -x "dist/$platform/pandapi-parser"; test -x "dist/$platform/pandaPIparser"` | serious | Arc03 command naming contract | open | | Mechanism is CC's decision. |
| F-3 | Parser contract fixture records exist for canonical and inherited parser commands, file-backed and stdout-backed output, positive and negative parser outcomes, status fields, normalization, and safety. | `set -e; for term in pandapi-parser pandaPIparser parser-contract "file-backed" "stdout-backed" input_unavailable output_unavailable input_invalid cli_usage_error PANDAPI_STATUS normalization safety; do rg -n -- "$term" fixtures/contract >/dev/null; done` | serious | Arc03 matrix and Slice02 scaffold | open | | May extend existing docs/records or add parser-specific records. |
| F-4 | The fixture runner supports parser contract selection while retaining baseline mode. | `set -e; test -x scripts/run-contract-fixtures.sh; bash -n scripts/run-contract-fixtures.sh; ./scripts/run-contract-fixtures.sh --list | rg -n -- "parser" >/dev/null; ./scripts/run-contract-fixtures.sh --contract --component parser --list | rg -n -- "parser" >/dev/null; ./scripts/run-contract-fixtures.sh --baseline --component parser >/dev/null` | serious | executable contract gate | open | | If the runner interface differs, amend the ledger before implementation. |
| F-5 | Canonical parser file-backed success conforms: `pandapi-parser --status=stderr --output OUT.htn DOMAIN.hddl PROBLEM.hddl` exits 0, completes the `.htn` artifact, keeps stdout empty, and emits final status `ok` on stderr. | `./scripts/run-contract-fixtures.sh --contract --component parser --case parser-canonical-file-success` | serious | parser positive file-backed contract | open | | Fixture should assert required status fields, not diagnostic prose. |
| F-6 | Canonical parser stdout-backed success conforms: `pandapi-parser --status=stderr --output - DOMAIN.hddl PROBLEM.hddl` exits 0, writes only the `.htn` artifact to stdout, emits final status on stderr, and creates no file artifact unless requested. | `./scripts/run-contract-fixtures.sh --contract --component parser --case parser-canonical-stdout-success` | serious | parser positive stdout contract | open | | No status or human prose may contaminate stdout artifact output. |
| F-7 | Inherited `pandaPIparser` compatibility conforms on supported normal parse paths with the same status/stream/artifact behavior as canonical invocation and no default deprecation warning. | `./scripts/run-contract-fixtures.sh --contract --component parser --case parser-inherited-compat-success` | serious | compatibility contract | open | | Positional output compatibility may be tested here or in an adjacent case. |
| F-8 | Parser help, version, and provenance commands use stdout informational output, exit `ok` / 0, do not process input, include canonical command identity, and keep inherited invocation compatibility visible where required. | `./scripts/run-contract-fixtures.sh --contract --component parser --case parser-info-commands` | serious | Arc03 informational command contract | open | | Stable field names matter more than host-specific values. |
| F-9 | Parser CLI usage errors map to `cli_usage_error` / exit 10 before input/model processing, including missing required args, unknown option, malformed option value, too many positionals, `--output` conflict, and illegal `--status=stdout` with stdout artifact or informational output. | `./scripts/run-contract-fixtures.sh --contract --component parser --case parser-cli-usage-errors` | serious | Arc03 parse-error contract | open | | Fixtures should prove no output artifact is created. |
| F-10 | Missing parser domain/problem/include inputs map to `input_unavailable` / exit 20, emit final status when requested, keep stdout non-artifact/empty as appropriate, and do not create an output artifact. | `./scripts/run-contract-fixtures.sh --contract --component parser --case parser-input-unavailable` | serious | input failure contract | open | | Include path/operation fields where feasible. |
| F-11 | Malformed or semantically invalid HDDL parser inputs map to `input_invalid` / exit 22 and stay distinct from missing input, unavailable output, timeout, dependency failure, and internal error. | `./scripts/run-contract-fixtures.sh --contract --component parser --case parser-input-invalid` | serious | invalid input contract | open | | Broken syntax and broken reference should both be covered. |
| F-12 | Parser unavailable output paths map to `output_unavailable` / exit 21 with an explicit artifact/partial-output policy and no successful artifact claim. | `./scripts/run-contract-fixtures.sh --contract --component parser --case parser-output-unavailable` | serious | output failure contract | open | | Use deterministic CI-safe output failures. |
| F-13 | Final `PANDAPI_STATUS` records are emitted exactly once when enabled, after artifact disposition is known, with required fields `status`, `component`, `surface`, `surface_disposition`, `exit_code`, and `class`. | `./scripts/run-contract-fixtures.sh --contract --component parser --case parser-final-status-fields` | serious | status contract | open | | Tagged text, not JSON. |
| F-14 | Parser status stream legality is enforced: `--status=stderr` works with file and stdout artifacts, `--status=stdout` works only when stdout is otherwise empty, and illegal stdout-status combinations are `cli_usage_error` / 10. | `./scripts/run-contract-fixtures.sh --contract --component parser --case parser-status-stream-legality` | correctness-grade | stdout/status ownership | open | | |
| F-15 | Parser color/TTY policy prevents ANSI leakage into stdout artifacts, tagged status, supervised output, machine output, pipes, files, and no-TTY output; `--no-color`, `--no-colour`, and `NO_COLOR` disable ANSI. | `./scripts/run-contract-fixtures.sh --contract --component parser --case parser-color-policy` | correctness-grade | Arc03 color contract | open | | Human TTY color may remain semantic/predicate tested. |
| F-16 | Visible parser verifier/output helper surfaces are fenced as `legacy_surface` / exit 31, or the closing report documents that they are not visible through the new supported parser CLI. | `set -e; ./scripts/run-contract-fixtures.sh --contract --component parser --case parser-legacy-surface-fencing; rg -n -- "parser verifier|output helper|legacy_surface|not visible" docs/design-v0.3.0/arc05-binary-contract-adoption/slice03-parser-contract-adoption/closing-report.md fixtures/contract >/dev/null` | serious | supported-surface boundary | open | | Do not expand helper paths into supported product behavior. |
| F-17 | Parser contract adoption is represented in CI or the closing report records a concrete blocker and re-entry condition. | `set -e; if rg -n -- "run-contract-fixtures.sh.*--contract.*parser|make .*contract.*parser|parser contract" .github/workflows mk scripts >/dev/null; then true; else rg -n -- "CI|parser contract|re-entry|blocker" docs/design-v0.3.0/arc05-binary-contract-adoption/slice03-parser-contract-adoption/closing-report.md >/dev/null; fi` | correctness-grade | CI runway | open | | Accept either direct workflow command or make target routed by workflow. |
| F-18 | Existing baseline fixture gate still passes after parser adoption, with grounder, engine, and pipeline remaining baseline-only. | `./scripts/run-contract-fixtures.sh --baseline` | serious | regression proof | open | | Baseline expectations may be deliberately updated only with explanation. |
| F-19 | Existing runtime quality gates, all-build, positive smoke, and negative smoke pass after parser adoption. | `set -e; ./scripts/check-format-owned.sh; ./scripts/build-runtime.sh; ./scripts/sanitize-runtime.sh; ./scripts/build-all.sh; ./scripts/smoke-test.sh; ./scripts/smoke-test.sh --negative` | serious | no behavior drift proof | open | | |
| F-20 | Workflow syntax is checked with actionlint if available, or the closing report records that local actionlint was unavailable and CI syntax validation remains delegated. | `set -e; if command -v actionlint >/dev/null 2>&1; then actionlint -color; else rg -n -- "actionlint" docs/design-v0.3.0/arc05-binary-contract-adoption/slice03-parser-contract-adoption/closing-report.md .github/workflows >/dev/null; fi` | correctness-grade | workflow hygiene | open | | |
| F-21 | The implementation does not add a network fetch, vendored package, held/rejected dependency import, or third-party API leakage into planner internals. | `set -e; ! { { git diff -U0 -- pandaPI/pandaPIparser pandapi-runtime fixtures scripts mk .github; git diff --cached -U0 -- pandaPI/pandaPIparser pandapi-runtime fixtures scripts mk .github; } | rg -n -e "FetchContent" -e "ExternalProject" -e "curl" -e "git clone" -e "find_package\\(nlohmann" -e "find_package\\(Boost" -e "find_package\\(absl" -e "find_package\\(GSL" -e "#include <nlohmann" -e "boost/" -e "absl/" -e "gsl/"; }` | serious | Arc02 dependency gates | open | | If CLI11/fmt/Catch2/tl::expected enters, closing report must prove its accepted gate. |
| F-22 | The slice does not modify grounder or engine source, release/package/publish scripts, tracked release assets, checksums, manifests, wolong-facing release shape, or create Arc05 close/CDC files. | `set -e; test -z "$({ git diff --name-only -- pandaPI/pandaPIgrounder pandaPI/pandaPIengine release release-manifest.txt SHA256SUMS scripts/package-release.sh scripts/publish-release.sh; git diff --cached --name-only -- pandaPI/pandaPIgrounder pandaPI/pandaPIengine release release-manifest.txt SHA256SUMS scripts/package-release.sh scripts/publish-release.sh; } | tr -d '\n')"; ! test -e docs/design-v0.3.0/arc05-binary-contract-adoption/closing-report.md; ! test -e docs/design-v0.3.0/arc05-binary-contract-adoption/slice03-parser-contract-adoption/cdc-verification.md` | serious | protected boundary | open | | Build scripts may produce ignored `dist/` output. |
| F-23 | Whitespace checks pass for unstaged and staged diffs. | `set -e; git diff --check; git diff --cached --check` | serious | repository hygiene | open | | |
| F-24 | CC writes a closing report with capability verdict, implementation decisions, verification, ledger walk, silent-drop check, and Bubble-up to Arc05. | `set -e; test -f docs/design-v0.3.0/arc05-binary-contract-adoption/slice03-parser-contract-adoption/closing-report.md; for term in "Capability Verdict" "Implementation Decisions" Verification "Ledger Walk" "Silent-Drop Check" "Bubble-up to Arc05"; do rg -n -- "$term" docs/design-v0.3.0/arc05-binary-contract-adoption/slice03-parser-contract-adoption/closing-report.md >/dev/null; done` | serious | ledger discipline | open | | |

## Notes

- Contract fixture IDs in this ledger are the planned interface. If CC finds a
  clearer runner interface while preserving the same substantive checks, amend
  the ledger before closing and explain the correction.
- Diagnostic prose may be asserted only as non-normative helper evidence.
  Process classification must come from exit code and `PANDAPI_STATUS` fields.
