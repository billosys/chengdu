# Slice 06: parser-native-contract-cutover

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | The slice open set records parser-only native cutover scope, Slice05 blocker context, grounder/engine deferral, no Bash adapter, no parser old-name artifacts, make-targeted verification, and release-publication boundary. | `set -e; test -f docs/design-v0.3.0/arc05-binary-contract-adoption/slice06-parser-native-contract-cutover/slice-doc.md; test -f docs/design-v0.3.0/arc05-binary-contract-adoption/slice06-parser-native-contract-cutover/cc-prompt.md; for term in "parser-only" "Slice05" "pandapi-parser" "Bash adapter" "pandaPIparser.legacy" "Grounder remains" "Engine remains" "make" "No release publication"; do rg -n -- "$term" docs/design-v0.3.0/arc05-binary-contract-adoption/slice06-parser-native-contract-cutover/slice-doc.md docs/design-v0.3.0/arc05-binary-contract-adoption/slice06-parser-native-contract-cutover/cc-prompt.md >/dev/null; done` | serious | Slice05 bubble-up | open | | |
| F-2 | Parser build output installs executable `pandapi-parser` and does not install `pandaPIparser` or `pandaPIparser.legacy` in `dist/<platform>/`. | `set -e; make build-parser; . tools/shared/platform; platform="$(detect_platform)"; test -x "dist/$platform/pandapi-parser"; ! test -e "dist/$platform/pandaPIparser"; ! test -e "dist/$platform/pandaPIparser.legacy"` | serious | no inherited parser compatibility | open | | |
| F-3 | `dist/<platform>/pandapi-parser` is not a shell script or copied Bash adapter. | `set -e; make build-parser; . tools/shared/platform; platform="$(detect_platform)"; file "dist/$platform/pandapi-parser" | rg -v 'shell script|text executable|ASCII text|UTF-8 text' >/dev/null; if head -c 2 "dist/$platform/pandapi-parser" 2>/dev/null | rg -q '^#!'; then exit 1; fi` | serious | Slice05 stop condition | open | | |
| F-4 | Parser managed-process options are handled natively by `pandapi-parser`, including `--status=stderr`, `--status=stdout`, `--supervised`, `--output`, `--help`, `--version`, and `--provenance`. | `set -e; make test-contract-parser-managed; ./tests/contract/run --contract --component parser --case parser_help; ./tests/contract/run --contract --component parser --case parser_version; ./tests/contract/run --contract --component parser --case parser_provenance` | serious | Arc03 contract | open | | Case names may be amended if the fixture manifest uses different parser record names. |
| F-5 | Parser legacy/helper surfaces remain fenced with managed status instead of silently passing through to inherited helper behavior. | `set -e; make test-contract-parser-managed; ./tests/contract/run --contract --component parser --case parser_legacy_surface || ./tests/contract/run --contract --component parser --case parser_legacy_helper` | serious | Arc03 supported surface | open | | Amend the exact case name if the fixture records use a more specific identifier. |
| F-6 | Active build/test/tooling surfaces do not invoke or install `scripts/pandapi-parser-adapter.sh`, `pandaPIparser`, or `pandaPIparser.legacy` as parser artifacts. | `set -e; ! rg -n -- 'pandapi-parser-adapter|pandaPIparser\\.legacy|cp .*pandaPIparser|pandaPIparser remains executable|legacy_binary=pandaPIparser' Makefile mk tests tools fixtures/contract README.md` | serious | parser shim retirement | open | | Source paths under `pandaPI/pandaPIparser` are allowed if not presented as command artifacts. |
| F-7 | `scripts/pandapi-parser-adapter.sh` is deleted; grounder transition shim scripts may remain. | `set -e; ! test -e scripts/pandapi-parser-adapter.sh; test -e scripts/pandapi-grounder-adapter.sh; test -e scripts/install-grounder-adapter.sh` | serious | parser shim retirement | open | | |
| F-8 | Parser fixture records no longer assert inherited parser command compatibility, parser `.legacy` sidecars, or parser adapter provenance. | `set -e; rg -n -- 'pandapi-parser' fixtures/contract/parser-contract-records.md >/dev/null; ! rg -n -- 'pandaPIparser|pandaPIparser\\.legacy|pandapi-parser-adapter|legacy_binary=pandaPIparser|remains executable' fixtures/contract/parser-contract-records.md fixtures/contract/README.md` | serious | executable contract records | open | | |
| F-9 | Parser provenance/version output names `pandapi-parser` as the canonical command and does not advertise a parser `legacy_binary` sidecar. | `set -e; make build-parser; . tools/shared/platform; platform="$(detect_platform)"; "dist/$platform/pandapi-parser" --provenance > /tmp/pandapi-parser-provenance.txt; rg -n -- 'canonical_command=pandapi-parser|component=parser|managed_process_contract=0.3.0' /tmp/pandapi-parser-provenance.txt >/dev/null; ! rg -n -- 'legacy_binary=pandaPIparser|invoked_command=pandaPIparser' /tmp/pandapi-parser-provenance.txt` | serious | provenance contract | open | | |
| F-10 | Parser managed contract fixtures and parser-relevant smoke coverage pass through make after the native cutover. | `set -e; make test-contract-parser-managed; make smoke; make smoke-negative` | serious | behavior preservation | open | | Grounder may still be transitional; this row proves parser did not regress pipeline use. |
| F-11 | `make test` passes with parser native and grounder still transitional. | `make test` | serious | release-branch quality gate | open | | If unrelated environment interference blocks the aggregate gate, record exact failing target and rerun narrower parser/gate targets. |
| F-12 | The implementation does not modify grounder source, engine source, release publication workflow, checksums, final release manifests, Arc05 close files, or CDC files. | `set -e; test -z "$({ git diff --name-only -- pandaPI/pandaPIgrounder pandaPI/pandaPIengine .github/workflows/release.yml release-manifest.txt SHA256SUMS docs/design-v0.3.0/arc05-binary-contract-adoption/closing-report.md docs/design-v0.3.0/arc05-binary-contract-adoption/slice06-parser-native-contract-cutover/cdc-verification.md; git diff --cached --name-only -- pandaPI/pandaPIgrounder pandaPI/pandaPIengine .github/workflows/release.yml release-manifest.txt SHA256SUMS docs/design-v0.3.0/arc05-binary-contract-adoption/closing-report.md docs/design-v0.3.0/arc05-binary-contract-adoption/slice06-parser-native-contract-cutover/cdc-verification.md; } | tr -d '\n')"` | serious | protected boundary | open | | |
| F-13 | The implementation adds no default-path network fetch, vendored package, held/rejected dependency import, or third-party API leakage into planner internals. | `set -e; ! { { git diff -U0 -- pandaPI/pandaPIparser pandapi-runtime fixtures mk tests tools scripts .github README.md; git diff --cached -U0 -- pandaPI/pandaPIparser pandapi-runtime fixtures mk tests tools scripts .github README.md; } | rg -n -e "FetchContent" -e "ExternalProject" -e "curl" -e "git clone" -e "find_package\\(nlohmann" -e "find_package\\(Boost" -e "find_package\\(absl" -e "find_package\\(GSL" -e "#include <nlohmann" -e "boost/" -e "absl/" -e "gsl/"; }` | serious | Arc02 dependency gates | open | | |
| F-14 | Staged and unstaged whitespace checks pass. | `set -e; git diff --check; git diff --cached --check` | serious | repository hygiene | open | | |
| F-15 | CC writes a closing report with capability verdict, implementation decisions, verification, ledger walk, silent-drop check, and Bubble-up to Arc05. | `set -e; test -f docs/design-v0.3.0/arc05-binary-contract-adoption/slice06-parser-native-contract-cutover/closing-report.md; for term in "Capability Verdict" "Implementation Decisions" Verification "Ledger Walk" "Silent-Drop Check" "Bubble-up to Arc05"; do rg -n -- "$term" docs/design-v0.3.0/arc05-binary-contract-adoption/slice06-parser-native-contract-cutover/closing-report.md >/dev/null; done` | serious | ledger discipline | open | | |

## Notes

- This is the parser half of the Slice05 remediation. Do not remove grounder
  shims here.
- New GitHub Actions / CI jobs must invoke make targets, not project scripts.
