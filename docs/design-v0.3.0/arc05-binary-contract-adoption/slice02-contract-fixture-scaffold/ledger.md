# Slice 02: contract-fixture-scaffold

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | The slice open set documents the contract-fixture scaffold scope, baseline-versus-contract boundary, no-binary-adoption boundary, fixture runner, CI, stdin/full-duplex caveat, and downstream parser/grounder/engine handoff. | `set -e; test -f docs/design-v0.3.0/arc05-binary-contract-adoption/slice02-contract-fixture-scaffold/slice-doc.md; for term in "contract fixture" baseline "No parser" runner CI "full-duplex" parser grounder engine; do rg -n -- "$term" docs/design-v0.3.0/arc05-binary-contract-adoption/slice02-contract-fixture-scaffold/slice-doc.md >/dev/null; done` | serious | Arc05 slice02 planning | open | | |
| F-2 | A repo-level contract fixture home exists and documents fixture shape, ownership, comparison policy, safety policy, and how baseline observations differ from future contract expectations. | `set -e; test -d fixtures/contract; test -f fixtures/contract/README.md; for term in "Fixture" owner baseline contract stdout stderr safety comparison; do rg -n -- "$term" fixtures/contract/README.md >/dev/null; done` | serious | Arc03 matrix and Arc04 fixture handoff | open | | |
| F-3 | Fixture metadata or records include component, surface disposition, command/argv, stdin mode, stdout role, stderr role, artifact expectation, expected exit/status, final status policy, normalization, and safety fields. | `set -e; rg -n -- "component|surface_disposition|argv|stdin|stdout|stderr|artifact|exit|final_status|normalization|safety" fixtures/contract >/dev/null` | serious | Arc03 fixture schema | open | | |
| F-4 | The fixture scaffold records current inherited-binary baseline expectations separately from future managed-process contract expectations. | `set -e; rg -n -- "baseline" fixtures/contract scripts/run-contract-fixtures.sh >/dev/null; rg -n -- "contract" fixtures/contract scripts/run-contract-fixtures.sh >/dev/null` | serious | no-overclaim boundary | open | | |
| F-5 | A contract fixture runner exists, is executable, uses strict shell hygiene, supports listing or selecting fixtures, and produces labeled PASS/FAIL output. | `set -e; test -x scripts/run-contract-fixtures.sh; bash -n scripts/run-contract-fixtures.sh; rg -n -- "set -euo pipefail|--list|PASS|FAIL" scripts/run-contract-fixtures.sh >/dev/null` | serious | executable fixture gate | open | | |
| F-6 | The runner captures stdout, stderr, exit status, and generated artifact state separately rather than classifying by mixed diagnostic prose alone. | `set -e; for term in stdout stderr exit artifact; do rg -n -- "$term" scripts/run-contract-fixtures.sh fixtures/contract >/dev/null; done` | serious | process-boundary proof | open | | |
| F-7 | The runner uses isolated temporary work directories and cleans up generated artifacts unless a preserve-for-debug mode is explicitly selected. | `set -e; rg -n -- "mktemp|trap|cleanup|preserve" scripts/run-contract-fixtures.sh fixtures/contract >/dev/null` | correctness-grade | fixture safety | open | | |
| F-8 | Parser baseline probes cover at least a current successful parse and distinct missing-file, broken-syntax, and broken-reference outcomes. | `./scripts/run-contract-fixtures.sh --baseline --component parser` | serious | parser adoption baseline | open | | |
| F-9 | Grounder baseline probes cover at least current successful grounding from a generated `.htn` artifact and a missing or malformed `.htn` outcome. | `./scripts/run-contract-fixtures.sh --baseline --component grounder` | serious | grounder adoption baseline | open | | |
| F-10 | Engine baseline probes cover at least current solved and unsolvable outcomes, keeping unsolvable distinct from malformed input or generic failure. | `./scripts/run-contract-fixtures.sh --baseline --component engine` | serious | engine adoption baseline | open | | |
| F-11 | A pipeline baseline probe composes parser, grounder, and engine through temporary artifacts and reports the observed current outcome without claiming final contract conformance. | `./scripts/run-contract-fixtures.sh --baseline --component pipeline` | serious | Arc05 composition runway | open | | |
| F-12 | Baseline fixture output makes absence or presence of final `PANDAPI_STATUS` explicit so later binary slices can flip expectations deliberately. | `set -e; rg -n -- "PANDAPI_STATUS|final_status" fixtures/contract scripts/run-contract-fixtures.sh >/dev/null` | correctness-grade | status contract runway | open | | |
| F-13 | The fixture scaffold preserves the Arc03 distinction between black-box process fixtures and Catch2/runtime seam tests. | `set -e; rg -n -- "black-box|Catch2|seam" fixtures/contract docs/design-v0.3.0/arc05-binary-contract-adoption/slice02-contract-fixture-scaffold/closing-report.md >/dev/null` | serious | Arc03/ci-notes boundary | open | | |
| F-14 | The implementation does not add a new parser dependency, network fetch, vendored package, or held/rejected dependency import for fixture execution. | `set -e; ! { { git diff -U0 -- fixtures scripts pandapi-runtime .github; git diff --cached -U0 -- fixtures scripts pandapi-runtime .github; } | rg -n -e "FetchContent" -e "ExternalProject" -e "curl" -e "git clone" -e "find_package\\(CLI11" -e "find_package\\(fmt" -e "find_package\\(tl" -e "find_package\\(reproc" -e "find_package\\(nlohmann" -e "find_package\\(Boost" -e "find_package\\(absl" -e "find_package\\(GSL" -e "#include <CLI11" -e "#include <fmt" -e "expected\\.hpp" -e "tl/expected" -e "reproc" -e "nlohmann" -e "boost/" -e "absl/" -e "gsl/"; }` | serious | Arc02 dependency gates | open | | |
| F-15 | The fixture scaffold does not create canonical `pandapi-*` commands, wrappers, symlinks, copied binaries, renamed binaries, release entry points, or inherited-name migration. | `set -e; test -z "$({ git diff --name-only -- dist release release-manifest.txt scripts/package-release.sh scripts/publish-release.sh; git diff --cached --name-only -- dist release release-manifest.txt scripts/package-release.sh scripts/publish-release.sh; } | tr -d '\n')"; ! { { git diff --name-only; git diff --cached --name-only; } | rg -n 'pandapi-(parser|grounder|engine)'; }` | serious | no-binary-adoption boundary | open | | |
| F-16 | No parser, grounder, or engine source is edited by this slice. | `test -z "$({ git diff --name-only -- pandaPI; git diff --cached --name-only -- pandaPI; } | tr -d '\n')"` | serious | protected source boundary | open | | |
| F-17 | Existing runtime quality gates and all-build/smoke gates pass after the fixture scaffold lands. | `set -e; ./scripts/check-format-owned.sh; ./scripts/build-runtime.sh; ./scripts/sanitize-runtime.sh; ./scripts/build-all.sh; ./scripts/smoke-test.sh; ./scripts/smoke-test.sh --negative` | serious | no behavior drift proof | open | | |
| F-18 | CI either runs the new baseline fixture gate on reliable runners or the closing report records a specific blocker and re-entry condition. | `set -e; if rg -n -- "run-contract-fixtures.sh" .github/workflows >/dev/null; then rg -n -- "run-contract-fixtures.sh" .github/workflows/build-reusable.yml >/dev/null; else rg -n -- "CI|run-contract-fixtures.sh|re-entry" docs/design-v0.3.0/arc05-binary-contract-adoption/slice02-contract-fixture-scaffold/closing-report.md >/dev/null; fi` | correctness-grade | CI runway | open | | |
| F-19 | Workflow syntax is checked with actionlint if available, or the closing report records that local actionlint was unavailable and CI syntax validation remains delegated. | `set -e; if command -v actionlint >/dev/null 2>&1; then actionlint -color; else rg -n -- "actionlint" docs/design-v0.3.0/arc05-binary-contract-adoption/slice02-contract-fixture-scaffold/closing-report.md .github/workflows/build-reusable.yml >/dev/null; fi` | correctness-grade | workflow hygiene | open | | |
| F-20 | Whitespace checks pass for unstaged and staged diffs. | `set -e; git diff --check; git diff --cached --check` | serious | repository hygiene | open | | |
| F-21 | CC writes a closing report with capability verdict, implementation decisions, verification, ledger walk, silent-drop check, and Bubble-up to Arc05. | `set -e; test -f docs/design-v0.3.0/arc05-binary-contract-adoption/slice02-contract-fixture-scaffold/closing-report.md; for term in "Capability Verdict" "Implementation Decisions" Verification "Ledger Walk" "Silent-Drop Check" "Bubble-up to Arc05"; do rg -n -- "$term" docs/design-v0.3.0/arc05-binary-contract-adoption/slice02-contract-fixture-scaffold/closing-report.md >/dev/null; done` | serious | ledger discipline | open | | |
| F-22 | CC does not create CDC's independent verification file. | `! test -e docs/design-v0.3.0/arc05-binary-contract-adoption/slice02-contract-fixture-scaffold/cdc-verification.md` | serious | CDC/CC role separation | open | | |

## Notes

- The baseline runner may report current inherited behavior that does not yet
  match the accepted Arc03 contract. That is allowed only when labeled as
  baseline evidence.
- If a verify command needs correction during implementation, correct it in
  the ledger and explain why the corrected command preserves the same
  substantive gate.
