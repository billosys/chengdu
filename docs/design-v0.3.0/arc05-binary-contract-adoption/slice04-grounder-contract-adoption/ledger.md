# Slice 04: grounder-contract-adoption

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | The slice open set documents grounder-only contract adoption scope, canonical/inherited command proof, grounder positive/negative fixtures, H2 and `cpddl`/FAM fencing, parser regression, engine baseline handoff, and make-targeted CI. | `set -e; test -f docs/design-v0.3.0/arc05-binary-contract-adoption/slice04-grounder-contract-adoption/slice-doc.md; for term in grounder-only pandapi-grounder pandaPIgrounder H2 cpddl FAM "PANDAPI_STATUS" parser engine make; do rg -n -- "$term" docs/design-v0.3.0/arc05-binary-contract-adoption/slice04-grounder-contract-adoption/slice-doc.md >/dev/null; done` | serious | Arc05 slice04 planning | closed | Command passed on 2026-08-10. | |
| F-2 | Grounder build output provides both canonical `pandapi-grounder` and inherited `pandaPIgrounder` executables in `dist/<platform>/`. | `set -e; make build-grounder; . scripts/lib-platform.sh; platform="$(detect_platform)"; test -x "dist/$platform/pandapi-grounder"; test -x "dist/$platform/pandaPIgrounder"` | serious | Arc03 command naming contract | closed | Command passed on macos-arm64; build installed both managed entry points and retained `pandaPIgrounder.legacy`. | Mechanism is a chengdu-owned adapter around the inherited grounder. |
| F-3 | Grounder contract fixture records exist for canonical and inherited grounder commands, file-backed and stdout-backed `.sas` output, positive and negative outcomes, H2/`cpddl`/FAM fencing, status fields, normalization, and safety. | `set -e; test -f fixtures/contract/grounder-contract-records.md; for term in pandapi-grounder pandaPIgrounder grounder-contract "file-backed" "stdout-backed" input_unavailable output_unavailable input_invalid cli_usage_error experimental_surface legacy_surface H2 cpddl FAM PANDAPI_STATUS normalization safety; do rg -n -- "$term" fixtures/contract/grounder-contract-records.md >/dev/null; done` | serious | Arc03 matrix and Slice02 scaffold | closed | Command passed; `grounder-contract-records.md` records grounder contract cases plus comparison, normalization, and safety policy. | |
| F-4 | The fixture runner supports grounder contract selection while retaining baseline and parser contract modes. | `set -e; test -x scripts/run-contract-fixtures.sh; bash -n scripts/run-contract-fixtures.sh; ./scripts/run-contract-fixtures.sh --contract --component grounder --list | rg -n -- "grounder" >/dev/null; ./scripts/run-contract-fixtures.sh --contract --component parser --list | rg -n -- "parser" >/dev/null; ./scripts/run-contract-fixtures.sh --baseline --component grounder >/dev/null` | serious | executable contract gate | closed | Command passed; runner supports baseline, parser contract, and grounder contract selection. | Grounder-only baseline passed with 7 passed, 0 failed; full baseline remains F-20 evidence. |
| F-5 | Canonical grounder file-backed success conforms: `pandapi-grounder --status=stderr --output OUT.sas INPUT.htn` exits 0, completes the `.sas` artifact, keeps stdout empty, and emits final status `ok` on stderr. | `./scripts/run-contract-fixtures.sh --contract --component grounder --case grounder-canonical-file-success` | serious | grounder positive file-backed contract | closed | Covered by final grounder contract suite: 272 passed, 0 failed. | Fixture consumes checked parser-generated-equivalent `.htn`. |
| F-6 | Canonical grounder stdout-backed success conforms: `pandapi-grounder --status=stderr --output - INPUT.htn` exits 0, writes only the `.sas` artifact to stdout, emits final status on stderr, and creates no file artifact unless requested. | `./scripts/run-contract-fixtures.sh --contract --component grounder --case grounder-canonical-stdout-success` | serious | grounder positive stdout contract | closed | Covered by final grounder contract suite: 272 passed, 0 failed. | Fixture asserts no progress, statistics, ANSI, `PANDAPI_STATUS`, or human prose in stdout artifact. |
| F-7 | Inherited `pandaPIgrounder` compatibility conforms on supported normal grounding paths with the same status/stream/artifact behavior as canonical invocation and no default deprecation warning. | `./scripts/run-contract-fixtures.sh --contract --component grounder --case grounder-inherited-compat-success` | serious | compatibility contract | closed | Covered by final grounder contract suite: 272 passed, 0 failed. | Managed inherited invocation conforms; raw positional legacy passthrough remains available with no final status when no managed option is used. |
| F-8 | Grounder help, version, and provenance commands use stdout informational output, exit `ok` / 0, do not process input, include canonical command identity, and keep inherited invocation compatibility visible where required. | `./scripts/run-contract-fixtures.sh --contract --component grounder --case grounder-info-commands` | serious | Arc03 informational command contract | closed | Covered by final grounder contract suite: 272 passed, 0 failed. | Default help names H2 as experimental and `cpddl`/FAM as legacy, not supported normal behavior. |
| F-9 | Grounder CLI usage errors map to `cli_usage_error` / exit 10 before input/model processing, including missing required args, unknown option, malformed option value, too many positionals, `--output` conflict, and illegal `--status=stdout` combinations. | `./scripts/run-contract-fixtures.sh --contract --component grounder --case grounder-cli-usage-errors` | serious | Arc03 parse-error contract | closed | Covered by final grounder contract suite: 272 passed, 0 failed. | Fixtures assert no output artifact for artifact-bearing usage errors. |
| F-10 | Missing or unavailable grounder `.htn` inputs map to `input_unavailable` / exit 20, emit final status when requested, keep stdout non-artifact/empty as appropriate, and do not create an output artifact. | `./scripts/run-contract-fixtures.sh --contract --component grounder --case grounder-input-unavailable` | serious | input failure contract | closed | Covered by final grounder contract suite: 272 passed, 0 failed. | Missing input is covered; unreadable input was not used because portable permissions differ across local/CI hosts. |
| F-11 | Malformed or semantically invalid `.htn` grounder inputs map to `input_invalid` / exit 22 and stay distinct from missing input, unavailable output, timeout, resource limit, dependency failure, and internal error. | `./scripts/run-contract-fixtures.sh --contract --component grounder --case grounder-input-invalid` | serious | invalid input contract | closed | Covered by final grounder contract suite: 272 passed, 0 failed. | |
| F-12 | Grounder unavailable output paths map to `output_unavailable` / exit 21 with an explicit artifact/partial-output policy and no successful artifact claim. | `./scripts/run-contract-fixtures.sh --contract --component grounder --case grounder-output-unavailable` | serious | output failure contract | closed | Covered by final grounder contract suite: 272 passed, 0 failed. | Missing parent and output-directory cases are covered. |
| F-13 | Final `PANDAPI_STATUS` records are emitted exactly once when enabled, after artifact disposition is known, with required fields `status`, `component`, `surface`, `surface_disposition`, `exit_code`, and `class`. | `./scripts/run-contract-fixtures.sh --contract --component grounder --case grounder-final-status-fields` | serious | status contract | closed | Covered by final grounder contract suite: 272 passed, 0 failed. | Tagged text, not JSON. |
| F-14 | Grounder status stream legality is enforced: `--status=stderr` works with file and stdout artifacts, `--status=stdout` works only when stdout is otherwise empty, and illegal stdout-status combinations are `cli_usage_error` / 10. | `./scripts/run-contract-fixtures.sh --contract --component grounder --case grounder-status-stream-legality` | correctness-grade | stdout/status ownership | closed | Covered by final grounder contract suite: 272 passed, 0 failed. | |
| F-15 | Grounder color/TTY policy prevents ANSI leakage into stdout artifacts, tagged status, supervised output, machine output, pipes, files, and no-TTY output; `--no-color`, `--no-colour`, and `NO_COLOR` disable ANSI. | `./scripts/run-contract-fixtures.sh --contract --component grounder --case grounder-color-policy` | correctness-grade | Arc03 color contract | closed | Covered by final grounder contract suite: 272 passed, 0 failed. | Explicit `--color=always` is rejected on machine-owned streams before ambient `NO_COLOR` suppression. |
| F-16 | Visible H2 grounder surface is fenced as `experimental_surface` / exit 32, or the closing report documents that H2 is not visible through the new supported grounder CLI. | `set -e; ./scripts/run-contract-fixtures.sh --contract --component grounder --case grounder-h2-surface-fencing; rg -n -- "H2|experimental_surface|not visible" docs/design-v0.3.0/arc05-binary-contract-adoption/slice04-grounder-contract-adoption/closing-report.md fixtures/contract/grounder-contract-records.md >/dev/null` | serious | supported-surface boundary | closed | Focused case passed; H2 is fenced as `experimental_surface` / 32 and recorded in fixture records and closing report. | H2 is not supported normal grounding behavior. |
| F-17 | Visible `cpddl`/FAM grounder surfaces are fenced as `legacy_surface` / exit 31, or the closing report documents that they are not visible through the new supported grounder CLI. | `set -e; ./scripts/run-contract-fixtures.sh --contract --component grounder --case grounder-cpddl-fam-surface-fencing; rg -n -- "cpddl|FAM|legacy_surface|not visible" docs/design-v0.3.0/arc05-binary-contract-adoption/slice04-grounder-contract-adoption/closing-report.md fixtures/contract/grounder-contract-records.md >/dev/null` | serious | supported-surface boundary | closed | Focused case passed; `cpddl`/FAM surfaces are fenced as `legacy_surface` / 31 and recorded in fixture records and closing report. | `cpddl` remains an implementation dependency and inherited advanced path, not a promoted product surface. |
| F-18 | Grounder `domain_no_plan` semantics are assessed: if reachable through accepted grounder behavior, an executable fixture maps it to `domain_no_plan` / 2; otherwise the closing report records why it is not reachable in this slice and the re-entry condition. | `set -e; if ./scripts/run-contract-fixtures.sh --contract --component grounder --case grounder-domain-no-plan; then true; else rg -n -- "domain_no_plan|not reachable|re-entry" docs/design-v0.3.0/arc05-binary-contract-adoption/slice04-grounder-contract-adoption/closing-report.md >/dev/null; fi` | correctness-grade | Arc03 status taxonomy | closed | Command passed after the runner reported `grounder-domain-no-plan` is not reachable through accepted grounder-only semantics and the closing report recorded the re-entry condition. | No synthetic no-plan claim was forced. |
| F-19 | The accepted parser contract gate still passes after grounder adoption. | `make test-contract-parser-managed` | serious | parser regression proof | closed | Passed on macos-arm64: parser contract summary 300 passed, 0 failed. | Parser behavior is already accepted by Slice03 and did not drift. |
| F-20 | Existing baseline fixture gate still passes after grounder adoption, with engine and pipeline remaining baseline-only where not intentionally replaced by accepted parser/grounder contract evidence. | `./scripts/run-contract-fixtures.sh --baseline` | serious | regression proof | closed | Passed on macos-arm64: baseline summary 38 passed, 0 failed. | |
| F-21 | Grounder contract adoption is represented in make-targeted CI; new workflow behavior, if any, invokes `make` targets rather than project scripts directly. | `set -e; rg -n -- "test-contract-grounder-managed" mk Makefile >/dev/null; rg -n -- "test: .*test-contract-grounder-managed" mk/tests.mk >/dev/null; rg -n -- "make ci-linux|make ci-macos" .github/workflows/build-reusable.yml >/dev/null; ! rg -n -- "scripts/(run-contract-fixtures|build-grounder|install-grounder|build-all|smoke-test)" .github/workflows` | correctness-grade | CI runway and operator make-target rule | closed | Command passed; `make test` reaches `test-contract-grounder-managed`, and reusable workflows still enter through `make ci-linux` / `make ci-macos`. | Existing scripts remain behind make targets. |
| F-22 | Existing runtime quality gates, sanitizer runtime, all-build/test aggregate, positive smoke, negative smoke, format, and provenance gates pass after grounder adoption. | `set -e; make format-check; make test-runtime-sanitize; make test; make provenance-check` | serious | no behavior drift proof | closed | Passed on macos-arm64: format OK; sanitizer runtime 5/5; `make test` exit 0 with parser 300/0, grounder 272/0, baseline 38/0, smoke 5/0, negative smoke 4/0; provenance check passed. | `make test` includes managed parser and managed grounder contract gates. |
| F-23 | Workflow syntax is checked with actionlint through the make target if available, or the closing report records that local actionlint was unavailable and CI syntax validation remains delegated. | `set -e; if command -v actionlint >/dev/null 2>&1; then make actionlint; else rg -n -- "actionlint" docs/design-v0.3.0/arc05-binary-contract-adoption/slice04-grounder-contract-adoption/closing-report.md .github/workflows mk >/dev/null; fi` | correctness-grade | workflow hygiene | closed | Local `actionlint` was available and `make actionlint` passed. | |
| F-24 | The implementation does not add a network fetch, vendored package, held/rejected dependency import, or third-party API leakage into planner internals. | `set -e; ! { { git diff -U0 -- pandaPI/pandaPIgrounder pandapi-runtime fixtures scripts mk .github; git diff --cached -U0 -- pandaPI/pandaPIgrounder pandapi-runtime fixtures scripts mk .github; } | rg -n -e "FetchContent" -e "ExternalProject" -e "curl" -e "git clone" -e "find_package\\(nlohmann" -e "find_package\\(Boost" -e "find_package\\(absl" -e "find_package\\(GSL" -e "#include <nlohmann" -e "boost/" -e "absl/" -e "gsl/"; }` | serious | Arc02 dependency gates | closed | Command passed with no matches. | No CLI11, fmt, Catch2, `tl::expected`, package-manager bootstrap, or network fetch was adopted in this slice. |
| F-25 | The slice does not modify parser or engine source, release/package/publish scripts, tracked release assets, checksums, manifests, wolong-facing release shape, or create Arc05 close/CDC files. | `set -e; test -z "$({ git diff --name-only -- pandaPI/pandaPIparser pandaPI/pandaPIengine release release-manifest.txt SHA256SUMS scripts/package-release.sh scripts/publish-release.sh; git diff --cached --name-only -- pandaPI/pandaPIparser pandaPI/pandaPIengine release release-manifest.txt SHA256SUMS scripts/package-release.sh scripts/publish-release.sh; } | tr -d '\n')"; ! test -e docs/design-v0.3.0/arc05-binary-contract-adoption/closing-report.md; ! test -e docs/design-v0.3.0/arc05-binary-contract-adoption/slice04-grounder-contract-adoption/cdc-verification.md` | serious | protected boundary | closed | Command passed; no protected parser/engine/release/package/publish paths changed and no Arc05 close or Slice04 CDC file was created. | Build scripts produced ignored `dist/` output only. |
| F-26 | Whitespace checks pass for unstaged and staged diffs. | `set -e; git diff --check; git diff --cached --check` | serious | repository hygiene | closed | `git diff --check` and `git diff --cached --check` passed before commit. | |
| F-27 | CC writes a closing report with capability verdict, implementation decisions, verification, ledger walk, silent-drop check, and Bubble-up to Arc05. | `set -e; test -f docs/design-v0.3.0/arc05-binary-contract-adoption/slice04-grounder-contract-adoption/closing-report.md; for term in "Capability Verdict" "Implementation Decisions" Verification "Ledger Walk" "Silent-Drop Check" "Bubble-up to Arc05"; do rg -n -- "$term" docs/design-v0.3.0/arc05-binary-contract-adoption/slice04-grounder-contract-adoption/closing-report.md >/dev/null; done` | serious | ledger discipline | closed | Closing report added with required sections. | |

## Notes

- Contract fixture IDs in this ledger are the planned interface. If CC finds a
  clearer runner interface while preserving the same substantive checks, amend
  the ledger before closing and explain the correction.
- Diagnostic prose may be asserted only as non-normative helper evidence.
  Process classification must come from exit code and `PANDAPI_STATUS` fields.
- New GitHub Actions / CI jobs must invoke make targets, not project scripts.
