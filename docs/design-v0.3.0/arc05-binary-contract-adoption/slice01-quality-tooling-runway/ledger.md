# Slice 01: quality-tooling-runway

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | The slice open set documents the quality-tooling runway scope, no-binary-adoption boundary, owned-source formatting, sanitizer, CI, clang-tidy/compile-commands, and deferred coverage/TSan/release gates. | `set -e; test -f docs/design-v0.3.0/arc05-binary-contract-adoption/slice01-quality-tooling-runway/slice-doc.md; for term in "quality runway" "owned-source" sanitizer CI clang-tidy "compile_commands" coverage TSan "No parser" "Arc06"; do rg -n -- "$term" docs/design-v0.3.0/arc05-binary-contract-adoption/slice01-quality-tooling-runway/slice-doc.md >/dev/null; done` | serious | Arc05 slice01 planning | done | Open-set command passed. | |
| F-2 | A top-level `.clang-format` exists and is documented as applying first to chengdu-owned source/tests, not bulk vendored `pandaPI/` source. | `set -e; test -f .clang-format; rg -n -- "clang-format|owned|pandaPI|vendored" docs/design-v0.3.0/arc05-binary-contract-adoption/slice01-quality-tooling-runway/closing-report.md pandapi-runtime/README.md >/dev/null` | serious | `ci-notes.md` formatting recommendation | done | `.clang-format` exists; README and closing report document owned-source scope and no vendored bulk format. | |
| F-3 | The owned-source format check script exists, is executable, supports `clang-format` or `xcrun clang-format`, and targets owned runtime source/tests without scanning all `pandaPI/`. | `set -e; test -x scripts/check-format-owned.sh; bash -n scripts/check-format-owned.sh; rg -n -- "xcrun clang-format|clang-format" scripts/check-format-owned.sh >/dev/null; rg -n -- "pandapi-runtime" scripts/check-format-owned.sh >/dev/null; ! rg -n -- "pandaPI" scripts/check-format-owned.sh` | serious | CI/tooling runway | done | Script is executable, parses cleanly, locates `clang-format`, and targets `pandapi-runtime` paths only; command passed. | |
| F-4 | The owned-source format check passes. | `./scripts/check-format-owned.sh` | serious | formatting gate | done | `check-format-owned.sh: OK: 25 files`. | |
| F-5 | The runtime sanitizer script exists, is executable, uses strict shell hygiene, configures a dedicated sanitizer build directory, and applies ASan/UBSan compile and link flags. | `set -e; test -x scripts/sanitize-runtime.sh; bash -n scripts/sanitize-runtime.sh; rg -n -- "set -euo pipefail" scripts/sanitize-runtime.sh >/dev/null; rg -n -- "address,undefined|-fsanitize=address" scripts/sanitize-runtime.sh >/dev/null; rg -n -- "build/.+san|runtime.+san|sanitize" scripts/sanitize-runtime.sh >/dev/null` | serious | `ci-notes.md` sanitizer recommendation | done | Script is executable, parses cleanly, uses strict shell hygiene, and configures `build/runtime-sanitize/<platform>` with ASan/UBSan flags; command passed. | |
| F-6 | The runtime sanitizer script builds `pandapi-runtime` and runs CTest successfully. | `./scripts/sanitize-runtime.sh` | serious | dynamic bug-finding gate | done | Sanitizer build passed on macos-arm64; runtime CTest passed 5/5. | |
| F-7 | The runtime CMake configuration produces or preserves `compile_commands.json` for the runtime build path used by tooling. | `set -e; ./scripts/build-runtime.sh; find build/runtime -name compile_commands.json -print -quit | rg -n 'compile_commands.json'` | correctness-grade | clang-tidy/tooling runway | done | `./scripts/build-runtime.sh` passed and produced `build/runtime/macos-arm64/compile_commands.json`. | |
| F-8 | The implementation records the local and CI `clang-tidy` decision: implemented, non-blocking, or deferred behind an install/toolchain gate. | `set -e; rg -n -- "clang-tidy" docs/design-v0.3.0/arc05-binary-contract-adoption/slice01-quality-tooling-runway/closing-report.md >/dev/null; rg -n -- "clang-tidy" pandapi-runtime/README.md docs/design-v0.3.0/ci-notes.md >/dev/null` | correctness-grade | static-analysis runway | done | Local `clang-tidy` was unavailable; README and closing report record the installed-toolchain gate and future owned-source scope. | |
| F-9 | CI normal build gates include runtime build coverage before parser, grounder, and engine builds, or explicitly justify using `build-all` as the composed gate. | `set -e; rg -n -- "build-runtime.sh|build-all.sh" .github/workflows/build-reusable.yml >/dev/null; rg -n -- "Build runtime|Build all|build-runtime|build-all" .github/workflows/build-reusable.yml >/dev/null` | serious | pre-adoption CI coverage | done | `build-reusable.yml` runs `Build runtime` before parser, grounder, and engine on Linux and macOS jobs. | |
| F-10 | CI includes the owned-source format check if `clang-format` is reliably available or installed in the job. | `set -e; rg -n -- "check-format-owned.sh|clang-format" .github/workflows/build-reusable.yml >/dev/null` | serious | formatting CI gate | done | Linux CI installs `clang-format`; Linux and macOS CI run `./scripts/check-format-owned.sh`. | |
| F-11 | CI includes runtime ASan/UBSan sanitizer coverage on at least one reliable runner, or the closing report records a specific blocker and follow-up. | `set -e; rg -n -- "sanitize-runtime.sh|ASan|UBSan|sanitize" .github/workflows/build-reusable.yml docs/design-v0.3.0/arc05-binary-contract-adoption/slice01-quality-tooling-runway/closing-report.md >/dev/null` | serious | sanitizer CI gate | done | Linux CI runs `./scripts/sanitize-runtime.sh`; closing report records the ASan/UBSan runner decision. | |
| F-12 | `ci-notes.md` remains the design note and is not rewritten into an implementation log. | `git diff --name-only -- docs/design-v0.3.0/ci-notes.md` | correctness-grade | design-note boundary | done | Command returned no output; `ci-notes.md` was not edited. | Expected result: no output unless a narrow status pointer is intentionally added and justified. |
| F-13 | Coverage and TSan are explicitly deferred with re-entry conditions tied to fixture-backed binary adoption and representative subprocess/stream workloads. | `set -e; for term in coverage TSan "process fixtures" "representative" "Arc06"; do rg -n -- "$term" docs/design-v0.3.0/arc05-binary-contract-adoption/slice01-quality-tooling-runway/closing-report.md pandapi-runtime/README.md >/dev/null; done` | correctness-grade | agreed ordering | done | README and closing report defer coverage to fixture-backed binary adoption and TSan to representative subprocess/stream workloads; Arc06 release proof remains routed. | |
| F-14 | No parser, grounder, or engine source is edited by this slice. | `test -z "$(git diff --name-only -- pandaPI)"` | serious | no-binary-adoption boundary | done | Protected `pandaPI/` diff returned no output. | |
| F-15 | The slice does not edit release assets/manifests, historical pins, `dist`, `release`, package, or publish paths. | `test -z "$(git diff --name-only -- release-manifest.txt vendor.env pins.env dist release scripts/package-release.sh scripts/publish-release.sh)"` | serious | Arc06 boundary | done | Protected release/package/publish diff returned no output. | |
| F-16 | Existing runtime, all-build, positive smoke, and negative smoke gates pass after the tooling changes. | `set -e; ./scripts/build-runtime.sh; ./scripts/build-all.sh; ./scripts/smoke-test.sh; ./scripts/smoke-test.sh --negative` | serious | no behavior drift proof | done | Runtime CTest passed 5/5; `build-all` passed; positive smoke passed 5/0; negative smoke passed 4/0. | |
| F-17 | Workflow syntax is checked with actionlint if available, or the closing report records that local actionlint was unavailable and CI syntax validation remains to GitHub/actionlint job. | `set -e; if command -v actionlint >/dev/null 2>&1; then actionlint -color; else rg -n -- "actionlint" docs/design-v0.3.0/arc05-binary-contract-adoption/slice01-quality-tooling-runway/closing-report.md .github/workflows/build-reusable.yml >/dev/null; fi` | serious | workflow hygiene | done | Local `actionlint -color` passed. | |
| F-18 | Whitespace checks pass for unstaged and staged diffs. | `set -e; git diff --check; git diff --cached --check` | serious | repository hygiene | done | Unstaged and staged whitespace checks passed. | |
| F-19 | No prohibited dependency fetch/import mechanism is introduced in the new owned tooling or runtime surfaces. | `! git diff -U0 -- .clang-format scripts/check-format-owned.sh scripts/sanitize-runtime.sh pandapi-runtime | rg -n -e "FetchContent" -e "ExternalProject" -e "curl" -e "git clone" -e "find_package\\(CLI11" -e "find_package\\(fmt" -e "find_package\\(tl" -e "find_package\\(reproc" -e "find_package\\(nlohmann" -e "find_package\\(Boost" -e "find_package\\(absl" -e "find_package\\(GSL" -e "#include <CLI11" -e "#include <fmt" -e "expected.hpp" -e "tl/expected" -e "reproc" -e "nlohmann" -e "boost/" -e "absl/" -e "gsl/"` | serious | Arc02 dependency gates | done | Diff-scoped import/fetch scan returned no matches. | Corrected from the open command because the original scan included existing README dependency-decision prose that must mention held/pilot libraries; the corrected command preserves the substantive no-new-import/no-fetch gate. |
| F-20 | CC writes a closing report with capability verdict, implementation decisions, verification, ledger walk, silent-drop check, and Bubble-up to Arc05. | `set -e; test -f docs/design-v0.3.0/arc05-binary-contract-adoption/slice01-quality-tooling-runway/closing-report.md; for term in "Capability Verdict" "Implementation Decisions" Verification "Ledger Walk" "Silent-Drop Check" "Bubble-up to Arc05"; do rg -n -- "$term" docs/design-v0.3.0/arc05-binary-contract-adoption/slice01-quality-tooling-runway/closing-report.md >/dev/null; done` | serious | ledger discipline | done | Closing report exists with required sections; command passed. | |
| F-21 | CC does not create CDC's independent verification file. | `! test -e docs/design-v0.3.0/arc05-binary-contract-adoption/slice01-quality-tooling-runway/cdc-verification.md` | serious | CDC/CC role separation | done | No `cdc-verification.md` exists for this slice. | |

## Notes

- This slice is allowed to touch `.github/workflows/build-reusable.yml`
  because CI runway is the point of the slice.
- This slice is not allowed to touch `pandaPI/` or release/package/publish
  surfaces.
- If a verify command needs correction during implementation, correct it in
  the ledger and explain why the corrected command preserves the same
  substantive gate.
