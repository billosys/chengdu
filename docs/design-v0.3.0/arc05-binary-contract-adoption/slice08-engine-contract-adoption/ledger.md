# Slice 08: engine-contract-adoption

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | The slice open set records engine-only managed contract scope, Slice06/Slice07 native parser/grounder context, no inherited engine compatibility, optional engine surface fencing, make-targeted verification, and release-publication boundary. | `set -e; test -f docs/design-v0.3.0/arc05-binary-contract-adoption/slice08-engine-contract-adoption/slice-doc.md; test -f docs/design-v0.3.0/arc05-binary-contract-adoption/slice08-engine-contract-adoption/cc-prompt.md; for term in "engine-only" "Slice06" "Slice07" "pandapi-engine" "pandaPIengine" "interactive" "translation" "SAT" "BDD" "CUDD" "make" "No release publication"; do rg -n -- "$term" docs/design-v0.3.0/arc05-binary-contract-adoption/slice08-engine-contract-adoption/slice-doc.md docs/design-v0.3.0/arc05-binary-contract-adoption/slice08-engine-contract-adoption/cc-prompt.md >/dev/null; done` | serious | Slice07 bubble-up | proposed-done | Prompt and slice doc preserve the engine-only scope, Slice06/Slice07 regression context, optional-surface fencing, make-targeted verification, and release boundary. | |
| F-2 | Engine build output installs executable `pandapi-engine` and does not install `pandaPIengine` in `dist/<platform>/`. | `set -e; make build-engine; . tools/shared/platform; platform="$(detect_platform)"; test -x "dist/$platform/pandapi-engine"; ! test -e "dist/$platform/pandaPIengine"` | serious | no inherited engine compatibility | proposed-done | `make build-engine` passed on `macos-arm64`; post-build checks found executable `dist/macos-arm64/pandapi-engine` and no old-name engine artifact. | |
| F-3 | `dist/<platform>/pandapi-engine` is not a shell script or copied adapter. | `set -e; make build-engine; . tools/shared/platform; platform="$(detect_platform)"; file "dist/$platform/pandapi-engine" | rg -v 'shell script|text executable|ASCII text|UTF-8 text' >/dev/null; if head -c 2 "dist/$platform/pandapi-engine" 2>/dev/null | rg -q '^#!'; then exit 1; fi` | serious | canonical binary adoption | proposed-done | `file dist/macos-arm64/pandapi-engine` reported a native Mach-O executable and the shebang check passed. | |
| F-4 | Engine managed-process options are handled by `pandapi-engine`, including `--status=stderr`, `--status=stdout`, `--supervised`, `--output`, `--help`, `--version`, and `--provenance`. | `set -e; make test-contract-engine-managed; ./tests/contract/run --contract --component engine --case engine-info-commands; ./tests/contract/run --contract --component engine --case engine-status-stream-legality; ./tests/contract/run --contract --component engine --case engine-final-status-fields` | serious | Arc03 contract | proposed-done | `make test-contract-engine-managed` passed with `289 passed, 0 failed`, including info, status-stream, and final-status fixture cases. | |
| F-5 | Engine solved and no-plan normal-search outcomes are contract-classified as `ok` / 0 and `domain_no_plan` / 2 with correct artifact and status behavior. | `set -e; make test-contract-engine-managed; ./tests/contract/run --contract --component engine --case engine-canonical-file-success; ./tests/contract/run --contract --component engine --case engine-canonical-stdout-success; ./tests/contract/run --contract --component engine --case engine-domain-no-plan` | serious | Arc03 status taxonomy | proposed-done | Engine contract fixtures passed file-backed solved, stdout-backed solved, and no-plan `domain_no_plan` outcome cases. | |
| F-6 | Engine input/output failure surfaces are contract-classified: CLI usage errors, unavailable input, invalid input, unavailable output, status fields, stream legality, and color policy. | `set -e; make test-contract-engine-managed; for case in engine-cli-usage-errors engine-input-unavailable engine-input-invalid engine-output-unavailable engine-final-status-fields engine-status-stream-legality engine-color-policy; do ./tests/contract/run --contract --component engine --case "$case"; done` | serious | Arc03 process contract | proposed-done | Engine fixture run covered CLI usage, input unavailable, input invalid, output unavailable, final status fields, stream legality, and color policy with managed status records. | |
| F-7 | Interactive, translation, SAT, BDD, and CUDD engine surfaces are fenced as legacy/experimental/unsupported/future or documented as hidden, without being promoted to supported normal search behavior. | `set -e; make test-contract-engine-managed; for case in engine-interactive-surface-fencing engine-translation-surface-fencing engine-sat-surface-fencing engine-bdd-cudd-surface-fencing; do ./tests/contract/run --contract --component engine --case "$case" || rg -n -- "$case|interactive|translation|SAT|BDD|CUDD|hidden|not visible|re-entry" docs/design-v0.3.0/arc05-binary-contract-adoption/slice08-engine-contract-adoption/closing-report.md fixtures/contract/engine-contract-records.md >/dev/null; done` | serious | supported-surface boundary | proposed-done | Engine contract fixtures passed interactive `legacy_surface`, translation `experimental_surface`, SAT `unsupported_feature`, and BDD/CUDD `future_surface` cases. | |
| F-8 | Engine contract fixture records exist and no longer assert inherited engine command compatibility as accepted 0.3.0 behavior. | `set -e; test -f fixtures/contract/engine-contract-records.md; rg -n -- 'pandapi-engine|engine-contract|domain_no_plan|normal_search|PANDAPI_STATUS' fixtures/contract/engine-contract-records.md >/dev/null; ! rg -n -- 'pandaPIengine|invoked_command=pandaPIengine|legacy_binary=pandaPIengine|compatibility' fixtures/contract/engine-contract-records.md fixtures/contract/README.md` | serious | executable contract records | proposed-done | Engine contract records were added for canonical `pandapi-engine`; negative grep over fixture records and README passed. | |
| F-9 | Active build/test/tooling/package/provenance surfaces use `pandapi-engine` and do not install, package, smoke, or document inherited `pandaPIengine` as an active accepted command. | `set -e; rg -n -- 'pandapi-engine' Makefile mk tests tools fixtures/contract README.md >/dev/null; ! rg -n -- 'pandaPIengine|cp .*pandaPIengine|check_component "pandaPIengine"|pandaPIengine:' Makefile mk tests tools fixtures/contract README.md` | serious | engine rename adoption | proposed-done | Active build/test/tooling/package/provenance/fixture/README grep passed for canonical `pandapi-engine` and no active old-name engine command references. | Source paths under `pandaPI/pandaPIengine` remain allowed. |
| F-10 | Engine provenance/version output names `pandapi-engine` as the canonical command, does not advertise an inherited engine compatibility command, and records optional engine surface dispositions. | `set -e; make build-engine; . tools/shared/platform; platform="$(detect_platform)"; "dist/$platform/pandapi-engine" --provenance > /tmp/pandapi-engine-provenance.txt; rg -n -- 'canonical_command=pandapi-engine|component=engine|managed_process_contract=0.3.0|surface_interactive=legacy|surface_translation=experimental|surface_sat=unsupported|surface_bdd_cudd=future' /tmp/pandapi-engine-provenance.txt >/dev/null; ! rg -n -- 'legacy_binary=pandaPIengine|invoked_command=pandaPIengine' /tmp/pandapi-engine-provenance.txt` | serious | provenance contract | proposed-done | Provenance command emitted canonical engine fields and optional-surface dispositions; negative old-name provenance grep passed. | |
| F-11 | Parser and grounder native behavior remains preserved after engine adoption. | `set -e; make test-contract-parser-managed; make test-contract-grounder-managed; . tools/shared/platform; platform="$(detect_platform)"; test -x "dist/$platform/pandapi-parser"; test -x "dist/$platform/pandapi-grounder"; ! test -e "dist/$platform/pandaPIparser"; ! test -e "dist/$platform/pandaPIparser.legacy"; ! test -e "dist/$platform/pandaPIgrounder"; ! test -e "dist/$platform/pandaPIgrounder.legacy"` | serious | Slice06/Slice07 regression proof | proposed-done | Parser and grounder managed contract runs passed with `285 passed, 0 failed` and `256 passed, 0 failed`; native artifacts remain present and old-name parser/grounder artifacts remain absent. | |
| F-12 | The parser-to-grounder-to-engine pipeline and smoke coverage use canonical `pandapi-*` binaries. | `set -e; make smoke; make smoke-negative; rg -n -- 'pandapi-parser|pandapi-grounder|pandapi-engine' tests/smoke/run fixtures/contract/baseline-records.md >/dev/null; ! rg -n -- 'pandaPIengine' tests/smoke/run fixtures/contract/baseline-records.md` | serious | full primary pipeline behavior | proposed-done | `make smoke` passed with `3 passed, 0 failed`; `make smoke-negative` passed with `4 passed, 0 failed`; smoke and baseline records use canonical `pandapi-*` commands. | |
| F-13 | `make test` passes with parser, grounder, and engine native. | `make test` | serious | release-branch quality gate | proposed-done | `make test` passed locally on `macos-arm64` with native parser, grounder, and engine contract/smoke gates. | |
| F-14 | Workflow syntax and make-target CI routing remain valid; new workflow behavior, if any, invokes make targets rather than project scripts directly. | `set -e; make actionlint; rg -n -- 'make ci-linux|make ci-macos|make actionlint' .github/workflows/build-reusable.yml >/dev/null; ! rg -n -- 'scripts/(run-contract-fixtures|build-parser|build-grounder|build-engine|build-all|smoke-test)' .github/workflows` | correctness-grade | operator make-target rule | proposed-done | `make actionlint` passed and workflow routing grep preserved make-target entrypoints without direct project-script calls. | |
| F-15 | The implementation does not modify release publication workflow, checksums, final release manifests, Arc05 close files, or CDC files. | `set -e; test -z "$({ git diff --name-only -- .github/workflows/release.yml release-manifest.txt SHA256SUMS docs/design-v0.3.0/arc05-binary-contract-adoption/closing-report.md docs/design-v0.3.0/arc05-binary-contract-adoption/slice08-engine-contract-adoption/cdc-verification.md; git diff --cached --name-only -- .github/workflows/release.yml release-manifest.txt SHA256SUMS docs/design-v0.3.0/arc05-binary-contract-adoption/closing-report.md docs/design-v0.3.0/arc05-binary-contract-adoption/slice08-engine-contract-adoption/cdc-verification.md; } | tr -d '\n')"` | serious | protected boundary | proposed-done | Protected-boundary diff command passed; no release publication workflow, checksum, final manifest, Arc05 close, or CDC files were changed. | |
| F-16 | The implementation adds no default-path network fetch, vendored package, held/rejected dependency import, or third-party API leakage into planner internals. | `set -e; ! { { git diff -U0 -- pandaPI/pandaPIengine pandapi-runtime fixtures mk tests tools scripts .github README.md; git diff --cached -U0 -- pandaPI/pandaPIengine pandapi-runtime fixtures mk tests tools scripts .github README.md; } | rg -n -e "FetchContent" -e "ExternalProject" -e "curl" -e "git clone" -e "find_package\\(nlohmann" -e "find_package\\(Boost" -e "find_package\\(absl" -e "find_package\\(GSL" -e "#include <nlohmann" -e "boost/" -e "absl/" -e "gsl/"; }` | serious | Arc02 dependency gates | proposed-done | Dependency/network grep passed; implementation reused in-tree engine source and `pandapi-runtime` without new default-path dependency fetches or package imports. | |
| F-17 | Staged and unstaged whitespace checks pass. | `set -e; git diff --check; git diff --cached --check` | serious | repository hygiene | proposed-done | `git diff --check` and `git diff --cached --check` passed after staging. | |
| F-18 | CC writes a closing report with capability verdict, implementation decisions, verification, ledger walk, silent-drop check, and Bubble-up to Arc05. | `set -e; test -f docs/design-v0.3.0/arc05-binary-contract-adoption/slice08-engine-contract-adoption/closing-report.md; for term in "Capability Verdict" "Implementation Decisions" Verification "Ledger Walk" "Silent-Drop Check" "Bubble-up to Arc05"; do rg -n -- "$term" docs/design-v0.3.0/arc05-binary-contract-adoption/slice08-engine-contract-adoption/closing-report.md >/dev/null; done` | serious | ledger discipline | proposed-done | Closing report added with all required sections. | |
| F-19 | Implementation-internal engine child-driver mechanics are not externally reachable as an unmanaged public CLI path. | `set -e; make build-engine; . tools/shared/platform; platform="$(detect_platform)"; set +e; "dist/$platform/pandapi-engine" --status=stderr --pandapi-engine-legacy-driver fixtures/engine/minimal.sas > /tmp/pandapi-engine-sentinel.stdout 2> /tmp/pandapi-engine-sentinel.stderr; rc=$?; set -e; test "$rc" -eq 10; test ! -s /tmp/pandapi-engine-sentinel.stdout; test "$(rg -c '^PANDAPI_STATUS' /tmp/pandapi-engine-sentinel.stderr)" -eq 1; rg -n -- 'status=cli_usage_error.*exit_code=10.*class=caller_error' /tmp/pandapi-engine-sentinel.stderr >/dev/null; ./tests/contract/run --contract --component engine --case engine-internal-driver-sentinel-fencing` | serious | CDC review finding | done | CDC corrective patch removed the public sentinel branch, added a managed fixture, and reproduced the focused case: 10 passed, 0 failed. Full engine contract then passed with 299 passed, 0 failed. | Added during CDC verification, after initial review found a public hidden-driver bypass. |

## Notes

- This is the engine adoption slice, not Arc05 synthesis.
- Parser native behavior from Slice06 and grounder native behavior from
  Slice07 are protected regression surfaces.
- New GitHub Actions / CI jobs must invoke make targets, not project scripts.
- F-19 is a CDC corrective row added after CC close. It records that the
  legacy child-driver implementation detail must stay fenced by normal option
  parsing and never become an unmanaged public invocation path.
