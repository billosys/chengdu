# Slice 03: patch-dissolution

> Ledger per `LEDGER-DISCIPLINE.md` (v2.0), Section A. All rows open at
> slice start, 2026-08-07. Closer: CC. Verifier: CDC. This slice retires the
> 0.1.0 build-time patch bridge by turning those deltas into ordinary in-tree
> source history.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | Slice starts from `release/0.2.x` with slice02 CDC-verified and worktree clean. | `git branch --show-current`; `git status --short`; `test -f docs/design-v0.2.0/arc01-vendor-identity/slice02-in-tree-build/cdc-verification.md` | serious | arc-plan section 2 | done | attested: branch `release/0.2.x`; pre-implementation worktree clean; slice02 CDC file present; actual slice03 start commit `db2f8a45e1f4269d5f434aca00ecf1f79f35d86f`. | `dd1fb5d6` is the slice02 CDC verification commit; `db2f8a45` is the slice03-open commit built from it. |
| F-2 | OQ3 is implemented as durable extracted bliss source under `pandaPI/pandaPIgrounder/cpddl/third-party/bliss/`, and builds no longer unzip bliss during normal source builds. | `test -f pandaPI/pandaPIgrounder/cpddl/third-party/bliss/bliss.cc`; `rg 'unzip|bliss-0.73-memleak.patch|bliss-0.73-cxx11-string-literal.patch' pandaPI/pandaPIgrounder/cpddl/Makefile scripts/build-grounder.sh` has no active build recipe hits | serious | arc-plan OQ3 v1.3 | done | attested: implementation commit `0958e760f4b63f06dd6ff424ff7c472340d1e27b`; durable bliss source tracked under `third-party/bliss/`; `test -f .../bliss.cc` passed; final `rg` returned no matches. | |
| F-3 | The cpddl makefile bridge delta from `0002-makefile.patch` is represented in in-tree source/build metadata, without active submodule fetch or patch commands. | inspect `pandaPI/pandaPIgrounder/cpddl/Makefile`, `Makefile.include`, and `bin/Makefile`; `rg 'git submodule|git apply|patch -p| -lrt' pandaPI/pandaPIgrounder/cpddl/Makefile pandaPI/pandaPIgrounder/cpddl/Makefile.include pandaPI/pandaPIgrounder/cpddl/bin/Makefile` returns no active bridge residue | serious | slice-doc section 2 | done | attested: `0958e760`; cpddl/bin linker flags no longer include `-lrt`; former submodule recipes are local-file checks; final `rg` returned no matches. | |
| F-4 | The two macOS boruvka bridge deltas are represented in `third-party/boruvka/` source and build metadata. | `rg '#include <endian.h>|sem_timedwait| -lrt' pandaPI/pandaPIgrounder/cpddl/third-party/boruvka/Makefile pandaPI/pandaPIgrounder/cpddl/third-party/boruvka/test/Makefile pandaPI/pandaPIgrounder/cpddl/third-party/boruvka/src/msg-schema.c pandaPI/pandaPIgrounder/cpddl/third-party/boruvka/src/fifo-sem.c pandaPI/pandaPIgrounder/cpddl/third-party/boruvka/src/ring_queue.c` returns no active bridge residue | serious | slice-doc section 2 | done | attested: `0958e760`; boruvka source/build metadata contains the bridge behavior directly; final `rg` returned no matches. | |
| F-5 | `scripts/build-grounder.sh` no longer applies patches, tracks an applied patch list, or consumes `patches/`; it builds only copied in-tree source. | `rg 'patch -p|git apply|PATCHES_DIR|APPLIED_PATCHES|\\.patch' scripts/build-grounder.sh` returns no active patch-bridge hits; inspect grounder build flow | serious | arc-plan A5 precursor | done | attested: `0958e760`; script uses `prepare_build_source_copy`, builds cpddl deps/lib and grounder source, and appends grounder provenance with `patches=none`; final `rg` returned no matches. | |
| F-6 | Bridge patch files are retired as active inputs after their deltas become source history. | `test -z "$(git ls-files patches 'pandaPI/pandaPIgrounder/*.patch')"`; `rg 'patches/' scripts README.md .github/workflows` has no active build-path hits | serious | project-plan section 3; arc-plan A5 | done | attested: `0958e760`; `git ls-files patches 'pandaPI/pandaPIgrounder/*.patch'` returned empty; final `rg 'patches/' scripts README.md .github/workflows` returned no matches. | cpddl's bundled memleak patch file was also retired because its delta is now in durable bliss source and the tracked-input check includes it. |
| F-7 | Grounder provenance reports no build-time patches on supported platforms while component SHAs still match `pins.env` until slice04 re-anchors provenance. | clean build; `./scripts/check-provenance.sh`; inspect `dist/*/provenance.txt` grounder block has `patches=none`; inspect `scripts/check-provenance.sh` expected grounder patch list | serious | slice-doc section 2; arc-plan A5 | done | attested: clean local build passed; `./scripts/check-provenance.sh` passed; `dist/macos-arm64/provenance.txt` records grounder SHA `b04ff7bcca710ae80a1ce73b8c027f0224565afc` and `patches=none`; checker expects grounder `none`. | Slice04 owns replacing the historical SHA scheme. |
| F-8 | Full local source-build gate passes without planner-source network fetch or build-time patch application. | from clean `build/ dist/ release/`: `./scripts/build-parser.sh`; `./scripts/build-grounder.sh`; `./scripts/build-engine.sh`; `./scripts/check-provenance.sh`; `./scripts/smoke-test.sh`; `./scripts/smoke-test.sh --negative` | serious | arc-plan A4/A5 precursor | done | attested: from clean generated output, parser, grounder, engine, provenance, positive smoke, and negative smoke all passed; positive smoke `5 passed, 0 failed`; negative smoke `4 passed, 0 failed`; build scripts contain no `patch`, `git apply`, or planner-source fetch. | |
| F-9 | Builds still leave the vendored source tree clean; generated bliss, boruvka, cpddl, parser, grounder, and engine artifacts stay in ignored build/dist/release paths. | after full gate, `git status --short -- pandaPI` empty; `find pandaPI -type f \\( -name '*.o' -o -name '*.a' -o -name pandaPIparser -o -name pandaPIgrounder -o -name pandaPIengine \\) -print` shows no generated build outputs | serious | slice02 invariant | done | attested: after committing `0958e760`, `git status --short -- pandaPI` returned empty; generated-output `find` returned empty. | Durable bliss source is tracked; generated bliss executable/object/archive outputs are ignored. |
| F-10 | README and workflow/source-build documentation no longer describe active compatibility patches applied by scripts. | inspect README source-build notes and `.github/workflows/build-reusable.yml`; `rg 'compatibility patch|patch bridge|applied by script|patches remain' README.md .github/workflows` returns no stale active-build wording | correctness | slice-doc section 2 | done | attested: README now says former build-time compatibility deltas are normal in-tree source history; workflow source-build steps unchanged from slice02 no-fetch shape; final `rg` returned no matches. | |
| F-11 | Patch-dissolution provenance is present in git history for each bridge delta. | `for p in 0002-makefile.patch 0001-boruvka-endian.patch 0001-Removed-non-macos-call-in-unused-function.patch bliss-0.73-cxx11-string-literal.patch; do git log dd1fb5d6..HEAD --format='%H%n%s%n%B' -- pandaPI scripts README.md | rg "Dissolves-Patch: $p" >/dev/null || exit 1; done` | correctness | slice-doc section 3 | done | attested: the verification loop passed; implementation commit `0958e760` carries `Dissolves-Patch` trailers for all four bridge patch names. | Combined source-history commit; the commit message names every dissolved patch and reason. |
| F-12 | Shell scripts and workflow syntax remain lintable, and local packaging still preserves asset shape. | `bash -n scripts/*.sh`; `shellcheck scripts/*.sh`; `actionlint .github/workflows/*.yml`; `./scripts/package-release.sh v0.2.0-slice03-smoke`; `git diff --check` | polish | local quality gate | done | attested: `bash -n scripts/*.sh`, `shellcheck scripts/*.sh`, `actionlint .github/workflows/*.yml`, `git diff --check`, and post-commit `./scripts/package-release.sh v0.2.0-slice03-smoke` all passed; `release/` contains the macOS tarball, `SHA256SUMS`, `release-manifest.txt`, `THIRD-PARTY-LICENSES`, `notes.md`, and `PRERELEASE`. | Not published; `release/` remains ignored. |

## What Worked

- The slice02 disposable-copy mechanism made patch dissolution testable without
  ever compiling in `pandaPI/`.
- Treating bliss as durable source simplified the grounder script: no archive
  extraction, no source patching, no patch CSV.
- The `Dissolves-Patch` trailers give slice04 and CDC a compact audit trail
  for every retired bridge delta.

## Closure

Closed at implementation commit `0958e760f4b63f06dd6ff424ff7c472340d1e27b` on
2026-08-07. Verified by: pending CDC.
Rows: 12. Done: 12. Deferred: 0. No-op: 0.
