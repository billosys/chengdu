# Slice 04: release-publication

> Ledger per `LEDGER-DISCIPLINE.md` (v2.0), Section A. All rows open at
> slice start, 2026-08-08. Closer: CC. Verifier: CDC. This slice publishes
> `v0.2.0` and hands Arc02 to CDC for independent verification and arc close.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | Slice starts from `release/0.2.x` with Slice03 closed, CDC-verified, and the worktree clean. | `git branch --show-current`; `git status --short --branch`; `test -f docs/design-v0.2.0/arc02-upstream-absorption/slice03-parser-makefile-pr/closing-report.md`; `test -f docs/design-v0.2.0/arc02-upstream-absorption/slice03-parser-makefile-pr/cdc-verification.md` | serious | slice-doc section 2 | done | Start branch `release/0.2.x`; `git status --short --branch` clean apart from expected ahead-of-origin state; Slice03 closing report and CDC verification present; Slice03 CDC accepted close HEAD `8810395c6c77e28c85895d6289a78821f7fad585`. | Ahead-of-origin commits were expected until the release branch push. |
| F-2 | Release preflight is refreshed at implementation time: target repo is `billosys/chengdu`, workflows are active, no `v0.2.0` tag/release exists, and the intended release commit SHA is recorded. | `git remote -v`; `git tag --list 'v0.2.0'`; `gh api repos/billosys/chengdu/releases/tags/v0.2.0`; `gh api repos/billosys/chengdu/git/ref/tags/v0.2.0`; `gh api repos/billosys/chengdu/actions/workflows`; `git rev-parse HEAD` | serious | slice-doc section 2 | done | `origin git@github.com:billosys/chengdu.git`; no local `v0.2.0`; remote release and tag APIs returned 404 before tagging; workflows `build-reusable`, `build`, and `release` active; pre-prep HEAD `4a609c36e0084fa49d3257c2f28b4ece6845c073`; final release commit `d38e30d396d435d5a80992106f59b403c20909e9`. | 404 for tag/release was treated as the required success state before the cut. |
| F-3 | Release-facing docs are current for `v0.2.0` while preserving wolong's frozen four-command fetch shape and asset naming contract. | inspect `README.md`; `rg -n 'v0\.1\.0|v0\.2\.0|pandapi-v0\.2\.0|SHA256SUMS|THIRD-PARTY-LICENSES' README.md .github/release-notes.md.in docs/license-audit-v0.2.0.md`; compare README command count/shape before and after | correctness | project ledger P5; slice-doc sections 1/3 | done | Commit `d38e30d396d435d5a80992106f59b403c20909e9` updates README install examples to `v0.2.0` tarball URLs while preserving the frozen curl/checksum/extract/export command shape; `git diff --check` passed. | Historical `v0.1.0` wording remains only where explicitly historical. |
| F-4 | No planner source or release-shape changes are introduced by this slice unless a gate exposes a release-blocking defect and the operator accepts the scope change. | `git diff --name-only <slice-start>..HEAD`; inspect no changes under `pandaPI/` unless explicitly justified; inspect no unexpected changes to `.github/workflows/release.yml`, `scripts/package-release.sh`, `scripts/publish-release.sh`, asset names, or tarball contents | serious | slice-doc section 3 out-of-scope | done | `git diff --name-only 4a609c36..d38e30d3` showed `README.md` only before publication; release asset names verified exactly; no `pandaPI/`, workflow, package, or publish script changes. | Closeout docs are post-publication and are not part of the release tag. |
| F-5 | Full local no-fetch gate passes at the final release commit from clean ignored generated provenance: parser, grounder, engine, provenance, positive smoke, negative smoke, and `v0.2.0` package dry-run. | clear ignored generated output where practical; `./scripts/build-parser.sh`; `./scripts/build-grounder.sh`; `./scripts/build-engine.sh`; `./scripts/check-provenance.sh`; `./scripts/smoke-test.sh`; `./scripts/smoke-test.sh --negative`; `./scripts/package-release.sh v0.2.0` | serious | arc ledger A4/A5/A6 | done | Removed stale `dist/macos-arm64/provenance.txt`; parser, grounder, and engine builds passed for macOS arm64; `check-provenance.sh` passed for all 3 components; positive smoke passed 5/5; negative smoke passed 4/4; package dry-run built both manifests and `pandapi-v0.2.0-macos-arm64.tar.gz` locally. | Existing compiler warnings were non-fatal and unchanged by this slice. |
| F-6 | Static and hygiene checks pass before publication. | `bash -n scripts/*.sh`; `/bin/bash -n scripts/*.sh`; `shellcheck scripts/*.sh`; `actionlint .github/workflows/*.yml`; `git diff --check`; `find pandaPI -name .git -print`; `git ls-files -s pandaPI \| awk '$1 == "160000"'` | correctness | local quality gate; arc01 no-gitlink baseline | done | `bash -n`, `/bin/bash -n`, `shellcheck`, `actionlint`, and `git diff --check` all exited 0; nested `.git` scan and `160000` gitlink scan produced no output. | Confirms no hidden gitlinks or nested Git metadata under `pandaPI/`. |
| F-7 | The final release commit is pushed to `origin/release/0.2.x`, and the full branch build matrix for that exact commit is green before tagging. | `git push origin release/0.2.x`; `gh run list --workflow build.yml --branch release/0.2.x --json databaseId,headSha,status,conclusion,url,createdAt --limit 10`; `gh run watch <run-id> --exit-status`; `gh run view <run-id> --json headSha,status,conclusion,url` | serious | slice-doc section 4 one-way-door guard | done | Pushed `release/0.2.x` `8c373e90..d38e30d3`; branch build run `31294772117` for `d38e30d396d435d5a80992106f59b403c20909e9` completed `success`: https://github.com/billosys/chengdu/actions/runs/31294772117. | Tag was not created until this row was done. |
| F-8 | Tag `v0.2.0` is created and pushed only after F-1 through F-7 are done, and it points at the intended final release commit. | ledger inspection before tag; `git tag v0.2.0 <sha>` or annotated equivalent; `git push origin v0.2.0`; `git rev-parse v0.2.0^{commit}`; `gh api repos/billosys/chengdu/git/ref/tags/v0.2.0` | serious | project ledger P5; release workflow trigger | done | Created lightweight tag `v0.2.0` at `d38e30d396d435d5a80992106f59b403c20909e9`; `git rev-parse v0.2.0^{commit}` returned that SHA; pushed tag; remote ref `refs/tags/v0.2.0` object type `commit`, SHA `d38e30d396d435d5a80992106f59b403c20909e9`. | No tag or release replacement was performed. |
| F-9 | The tag-triggered release workflow for `v0.2.0` completes green and publishes a non-prerelease GitHub Release. | `gh run list --workflow release.yml --json databaseId,headSha,event,headBranch,status,conclusion,url,createdAt --limit 10`; `gh run watch <run-id> --exit-status`; `gh run view <run-id> --json headSha,status,conclusion,url`; `gh release view v0.2.0 --json tagName,isDraft,isPrerelease,publishedAt,url,targetCommitish` | serious | arc ledger A4/A6; project ledger P5 | done | Release run `31294905237` for tag `v0.2.0` / SHA `d38e30d396d435d5a80992106f59b403c20909e9` completed `success`: https://github.com/billosys/chengdu/actions/runs/31294905237; release `v0.2.0` published `2026-08-09T04:39:12Z`, `isDraft=false`, `isPrerelease=false`: https://github.com/billosys/chengdu/releases/tag/v0.2.0. | GitHub reports `targetCommitish=main`, but the remote tag ref is the release authority and points at the intended commit. |
| F-10 | The public `v0.2.0` asset set is exact: two tarballs, `SHA256SUMS`, `release-manifest.txt`, and `THIRD-PARTY-LICENSES`; no missing or unexpected assets. | `gh release view v0.2.0 --json assets`; compare asset names exactly: `pandapi-v0.2.0-linux-x86_64.tar.gz`, `pandapi-v0.2.0-macos-arm64.tar.gz`, `SHA256SUMS`, `release-manifest.txt`, `THIRD-PARTY-LICENSES` | serious | wolong frozen release shape; arc ledger A6 | done | Public assets exactly: `pandapi-v0.2.0-linux-x86_64.tar.gz`, `pandapi-v0.2.0-macos-arm64.tar.gz`, `SHA256SUMS`, `release-manifest.txt`, `THIRD-PARTY-LICENSES`; no `notes.md` asset. | Release notes are the body, not an uploaded asset. |
| F-11 | Downloaded release assets verify and reconcile: checksums pass; each tarball contains exactly the three binaries, `provenance.txt`, and `fixtures/`; manifest/provenance name the release commit, import identities, source prefixes, and `patches=none`. | in clean temp dir: `gh release download v0.2.0`; `sha256sum -c SHA256SUMS --ignore-missing` or `shasum -a 256 -c SHA256SUMS --ignore-missing`; `tar tzf pandapi-v0.2.0-linux-x86_64.tar.gz`; `tar tzf pandapi-v0.2.0-macos-arm64.tar.gz`; inspect `release-manifest.txt` and per-tarball `provenance.txt` | serious | arc ledger A5/A6; project ledger P2/P5 | done | Downloaded to `/tmp/chengdu-v020-assets.wIuLMu`; `shasum -a 256 -c SHA256SUMS --ignore-missing` passed for both tarballs, `release-manifest.txt`, and `THIRD-PARTY-LICENSES`; both tarballs contain `pandaPIparser`, `pandaPIgrounder`, `pandaPIengine`, `provenance.txt`, and `fixtures/domain.hddl` / `fixtures/problem.hddl`; manifest and tarball provenance name `d38e30d396d435d5a80992106f59b403c20909e9`, expected prefixes/import SHAs/import commits, and `patches=none`. | macOS provenance also records min OS 15.0 for all three binaries. |
| F-12 | A real macOS arm64 consumer walk succeeds from the public release using README's documented shape: download, checksum, extract, full parse-ground-solve-convert-verify chain ending `Plan verification result: true`. | clean temp dir; execute README macOS commands with `v0.2.0`; record transcript and exit code | serious | project ledger P5; wolong fetch spec | done | In `/tmp/chengdu-v020-consumer.a9bs8j`, downloaded public macOS tarball and `SHA256SUMS` via `curl -fLO`; checksum passed; extracted; `./pandaPIparser fixtures/domain.hddl fixtures/problem.hddl plan.htn`; `./pandaPIgrounder plan.htn plan.sas`; `./pandaPIengine plan.sas > plan.raw`; `./pandaPIparser -c plan.raw plan.txt`; `./pandaPIparser --verify fixtures/domain.hddl fixtures/problem.hddl plan.txt` exited 0 and printed `Plan verification result: true`. | CDC Linux consumer walk remains the independent handoff. |
| F-13 | Upstream courtesy announcement issues are filed and recorded for parser, engine, and grounder. | `gh issue create --repo panda-planner-dev/pandaPIengine ...`; `gh issue create --repo panda-planner-dev/pandaPIparser ...`; `gh issue create --repo panda-planner-dev/pandaPIgrounder ...`; record issue URLs in ledger and closing report | serious | project-plan section 3 upstream courtesy; arc ledger A7 | done | Filed engine: https://github.com/panda-planner-dev/pandaPIengine/issues/18; parser: https://github.com/panda-planner-dev/pandaPIparser/issues/34; grounder: https://github.com/panda-planner-dev/pandaPIgrounder/issues/15. | Each issue names release URL, import SHA, fork rationale, and relevant absorbed PR/issue or no-PR scope. |
| F-14 | Repo hygiene holds at close: worktree clean, no temporary release branch/worktree/remotes left behind, no tracked generated output, and no generated output under `pandaPI/`. | `git status --short --branch`; `git worktree list`; `git remote -v`; `git status --short -- pandaPI scripts .github README.md docs/design-v0.2.0/arc02-upstream-absorption/slice04-release-publication`; `find pandaPI -type f \( -name '*.o' -o -name pandaPIparser -o -name pandaPIgrounder -o -name pandaPIengine \) -print` | correctness | repo hygiene | done | Before closeout edits, `git status --short --branch` was clean and synced with origin; `git worktree list` showed only `/Users/oubiwann/lab/billosys/chengdu d38e30d3 [release/0.2.x]`; `git remote -v` showed only origin; scoped status and generated-file scan under `pandaPI/` produced no output. | This row will be rechecked after the closeout commit. Ignored `build/`, `dist/`, and `release/` may remain. |
| F-15 | Slice close report walks all 15 rows and includes Bubble-up to the arc/project: delivered-as-assigned, release URL/run URL/tag SHA, courtesy issue URLs, CDC Linux handoff, whether arc/project docs changed, and silent-drop diff. | `test -f docs/design-v0.2.0/arc02-upstream-absorption/slice04-release-publication/closing-report.md`; inspect row count and Bubble-up section | serious | PROJECT-MANAGEMENT Part IV | done | `closing-report.md` added with release commit/tag/run/release evidence, exact assets, checksum/tarball/provenance verification, macOS consumer summary, CDC Linux handoff, courtesy issue URLs, row-by-row closure, Bubble-up, and silent-drop diff. | CC did not write `cdc-verification.md`, Arc02 `closing-report.md`, or project close. |

## What Worked

- The one-way-door guard worked: local gates, then pushed branch matrix, then tag
  publication kept the release tag tied to a green public commit.
- Download-side verification caught the real consumer contract, not just local
  build artifacts: public tarball, checksum file, manifest, provenance, and
  README command shape all matched.

## Follow-ups / Deferrals

- CDC still owns independent Slice04 verification, the Linux consumer download
  walk, Arc02 close, and any project close/status synthesis.
- Closeout docs are intentionally post-publication and are not included in the
  `v0.2.0` tag, which points at release commit
  `d38e30d396d435d5a80992106f59b403c20909e9`.
