# Slice 03: parser-makefile-pr

> Ledger per `LEDGER-DISCIPLINE.md` (v2.0), Section A. All rows open at
> slice start, 2026-08-08. Closer: CC. Verifier: CDC. This slice absorbs
> upstream pandaPIparser PR #21 as the final selected upstream PR before
> release publication.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | Slice starts from `release/0.2.x` with Slice02 closed, CDC-verified, and the worktree clean. | `git branch --show-current`; `git status --short --branch`; `test -f docs/design-v0.2.0/arc02-upstream-absorption/slice02-engine-exit-status/closing-report.md`; `test -f docs/design-v0.2.0/arc02-upstream-absorption/slice02-engine-exit-status/cdc-verification.md` | serious | slice-doc section 1 | done | `git status --short --branch` at start: `## release/0.2.x...origin/release/0.2.x [ahead 1]`; Slice02 close and CDC verification files present. | Start was clean except the expected local slice-open docs commit. |
| F-2 | Upstream parser PR #21 is refreshed at implementation time, with state, head SHA, author identity, merge/applicability status, and changed-file summary recorded. | `gh api repos/panda-planner-dev/pandaPIparser/pulls/21`; `gh api repos/panda-planner-dev/pandaPIparser/pulls/21/commits`; `git apply --check --directory=pandaPI/pandaPIparser <pr21.diff>` or reversible cherry-pick check | serious | arc-plan section 2 | done | 2026-08-08 refresh: PR #21 open/non-draft; head `9735763ffd694d95c287766a447deba980d7deaa`; base `69016f1046ce221c14d82b7102de49a03e12c192`; mergeable clean/rebaseable; one commit by Robert P. Goldman `<rpgoldman@sift.net>`; one file, +6/-2; `git apply --check --directory=pandaPI/pandaPIparser /tmp/chengdu-parser-pr21.patch` exited 0. | Matches slice-open shape. |
| F-3 | PR #21 is absorbed into `pandaPI/pandaPIparser/makefile` by native author-preserving cherry-pick where possible; any manual port is justified. | `git log --format=fuller <slice-start>..HEAD -- pandaPI/pandaPIparser/makefile`; inspect author and commit body/trailers | serious | project-plan P3; arc-plan A2 | done | `git fetch https://github.com/panda-planner-dev/pandaPIparser.git refs/pull/21/head`; `git cherry-pick -Xsubtree=pandaPI/pandaPIparser FETCH_HEAD`; implementation commit `49c02f7f0cb7a68c9d30df66992c172e111b3641`, author Robert P. Goldman `<rpgoldman@sift.net>`. | Native cherry-pick succeeded; only message trailers were amended. |
| F-4 | The parser makefile change matches PR #21's bounded behavior: `all: pandaPIparser`, `pandaPIparser:` owns object dependencies, and `install: pandaPIparser` installs to `${DESTDIR}/bin`. | `git diff <slice-start>..HEAD -- pandaPI/pandaPIparser/makefile`; `rg -n '^all: pandaPIparser$|^pandaPIparser:|^install: pandaPIparser|install pandaPIparser' pandaPI/pandaPIparser/makefile` | correctness | PR #21 diff | done | `git diff --name-only e861c1fe..HEAD` showed only `pandaPI/pandaPIparser/makefile`; `rg` found `all: pandaPIparser` at line 26, `pandaPIparser:` at line 28, `install: pandaPIparser` at line 65, and `install pandaPIparser ${DESTDIR}/bin` at line 66. | No install-layout redesign or extra makefile cleanup. |
| F-5 | Normal chengdu parser build still succeeds from in-tree source via the ignored build-source copy and produces `dist/<platform>/pandaPIparser`. | `./scripts/build-parser.sh`; `PLATFORM="$(bash -c '. scripts/lib-platform.sh; detect_platform')"`; `test -x "dist/$PLATFORM/pandaPIparser"`; inspect parser provenance block | serious | project DoD; build regression guard | done | `./scripts/build-parser.sh` passed for `macos-arm64`; produced `/Users/oubiwann/lab/billosys/chengdu/dist/macos-arm64/pandaPIparser`; later provenance gate verified the parser against commit `49c02f7f0cb7a68c9d30df66992c172e111b3641`. | Existing compiler warnings only. |
| F-6 | The new parser `install` target works in a disposable destination without dirtying `pandaPI/`. | after `./scripts/build-parser.sh`: `PLATFORM="$(bash -c '. scripts/lib-platform.sh; detect_platform')"`; `tmp=$(mktemp -d)`; `mkdir -p "$tmp/bin"`; `make -C "build/$PLATFORM/source/pandaPIparser" DESTDIR="$tmp" install`; `test -x "$tmp/bin/pandaPIparser"`; `"$tmp/bin/pandaPIparser" --help`; `git status --short -- pandaPI` | serious | PR #21 title and implementation | done | Install smoke passed: `make -C build/macos-arm64/source/pandaPIparser DESTDIR=/var/folders/.../tmp.bXuOeXhI6Q install`; `test -x` passed; installed `pandaPIparser --help` printed usage and exited 0; `git status --short -- pandaPI` produced no output. | PR behavior requires `${DESTDIR}/bin` to exist; smoke created it as specified. |
| F-7 | Full local no-fetch source gate passes after the parser makefile change. | from clean generated output where practical: `./scripts/build-parser.sh`; `./scripts/build-grounder.sh`; `./scripts/build-engine.sh`; `./scripts/check-provenance.sh`; `./scripts/smoke-test.sh`; `./scripts/smoke-test.sh --negative` | serious | arc-plan A4/A5 | done | At implementation commit `49c02f7f0cb7a68c9d30df66992c172e111b3641`: parser, grounder, and engine builds passed; after clearing stale ignored `dist/macos-arm64/provenance.txt` and rebuilding all three, `./scripts/check-provenance.sh` passed; positive smoke passed `5 passed, 0 failed`; negative smoke passed `4 passed, 0 failed`. | Docs-close commit follows this gate; CDC should rebuild at close HEAD because provenance records Git `HEAD`. |
| F-8 | Package dry-run preserves wolong's frozen release shape and validates checksums/manifests after the parser makefile change. | `./scripts/package-release.sh v0.2.0-slice03-smoke`; inspect `release/`; checksum verification used by package script | serious | project ledger P5 precursor | done | `./scripts/package-release.sh v0.2.0-slice03-smoke` passed for `macos-arm64`; script built tarball with fixtures, `THIRD-PARTY-LICENSES`, `release-manifest.txt`, `SHA256SUMS`, and `notes.md`; no publication performed. | Dry-run only. |
| F-9 | Maintained shell/workflow/static checks pass and docs are diff-clean. | `bash -n scripts/*.sh`; `/bin/bash -n scripts/*.sh` where available; `shellcheck scripts/*.sh`; `actionlint .github/workflows/*.yml`; `git diff --check` | polish | local quality gate | done | `bash -n scripts/*.sh`, `/bin/bash -n scripts/*.sh`, `shellcheck scripts/*.sh`, `actionlint .github/workflows/*.yml`, and `git diff --check` all exited 0. | No shell or workflow changes were made. |
| F-10 | Non-selected upstream work remains outside the committed diff: engine PR #15, engine PR #9, grounder PR #7, grounder issue #8, and parser #15/#29 resource work stay no-op/deferred as triaged. | `git diff --name-only <slice-start>..HEAD`; inspect no changes under `pandaPI/pandaPIengine`, `pandaPI/pandaPIgrounder`, SAT verifier/vendor additions, wrapper scripts, Dockerfiles, parser resource-fix code paths | serious | arc-plan section 3 | done | `git diff --name-only e861c1fe..HEAD` showed only `pandaPI/pandaPIparser/makefile`. | Engine, grounder, SAT verifier, Docker, wrapper, and parser resource/normalization work remain deferred. |
| F-11 | Upstream credit is present in implementation commits: original author identity is preserved or Robert P. Goldman is explicitly credited, and upstream PR URL is named. | `git log --format=fuller <slice-start>..HEAD -- pandaPI/pandaPIparser`; inspect commit body/trailers for `Upstream-PR: https://github.com/panda-planner-dev/pandaPIparser/pull/21` and required co-author trailers | serious | project ledger P3; arc ledger A2 | done | `git log --format=fuller -1 49c02f7f0cb7a68c9d30df66992c172e111b3641` shows author Robert P. Goldman `<rpgoldman@sift.net>` plus `Upstream-PR: https://github.com/panda-planner-dev/pandaPIparser/pull/21`, `Co-authored-by: Codex <noreply@openai.com>`, and `Co-authored-by: Billo AI <ai-engineering@billo.systems>`. | Native author preservation succeeded. |
| F-12 | Repo hygiene holds at close: worktree clean, no generated outputs under `pandaPI/`, no temporary PR worktree/branch/remotes left behind. | `git status --short --branch`; `find pandaPI -name .git -print`; `git ls-files -s pandaPI | awk '$1 == "160000"'`; `git worktree list`; `git remote -v` | correctness | arc01 no-gitlink/no-generated baseline | done | Pre-doc-close hygiene: `git status --short --branch` was `## release/0.2.x...origin/release/0.2.x [ahead 2]`; `find pandaPI -name .git -print` and gitlink scan produced no output; `git worktree list` showed only this worktree at `49c02f7f`; `git remote -v` showed only `origin git@github.com:billosys/chengdu.git`. Final close commit hygiene rechecked after docs commit. | Ignored `build/`, `dist/`, and `release/` outputs remain untracked. |
| F-13 | Slice close report walks all 13 rows and includes Bubble-up to the arc/project: delivered-as-assigned, any install-target caveat, whether arc/project docs changed, and silent-drop diff. | `test -f docs/design-v0.2.0/arc02-upstream-absorption/slice03-parser-makefile-pr/closing-report.md`; inspect row count and Bubble-up section | serious | PROJECT-MANAGEMENT Part IV | done | `closing-report.md` added with summary, upstream refresh, implementation, verification, 13-row ledger walk, and Bubble-up. | No `cdc-verification.md` written. |

## What Worked

- Native `git cherry-pick -Xsubtree=pandaPI/pandaPIparser FETCH_HEAD`
  applied PR #21 cleanly and preserved Robert P. Goldman's author identity.
- The new install target works from the ignored build-source copy with a
  disposable `DESTDIR` and leaves tracked `pandaPI/` clean.
- Clearing stale ignored generated provenance before rebuilding restored a
  clean provenance/package gate at the implementation commit.

## Follow-ups / Deferrals

- CDC should rebuild generated artifacts at the docs-close HEAD before writing
  `cdc-verification.md`, because provenance records the current Git commit.
- Release publication remains for the next Arc02 slice.
- Engine PR #15, engine PR #9, grounder PR #7, grounder issue #8, and parser
  #15/#29 resource/normalization work remain deferred.
