# Fetched upstream checkouts and build output — reproducible from
# pins.env + scripts/, never committed.
/upstream/
/build/
/bin/
/dist/
/release/
.worktrees
/workbench

# Scratch
*.o
*.log
.DS_Store
