# Slice 05: canonical-binary-cutover

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | The slice open set records parser/grounder canonical cutover scope, no inherited-name compatibility policy, shim deletion, active-surface doc updates, engine deferral, make-targeted verification, and release-publication boundary. | `set -e; test -f docs/design-v0.3.0/arc05-binary-contract-adoption/slice05-canonical-binary-cutover/slice-doc.md; test -f docs/design-v0.3.0/arc05-binary-contract-adoption/slice05-canonical-binary-cutover/cc-prompt.md; for term in "pandapi-parser" "pandapi-grounder" "pandaPIparser" "pandaPIgrounder" shim "Engine remains" "make" "Arc08" "No release publication"; do rg -n -- "$term" docs/design-v0.3.0/arc05-binary-contract-adoption/slice05-canonical-binary-cutover/slice-doc.md docs/design-v0.3.0/arc05-binary-contract-adoption/slice05-canonical-binary-cutover/cc-prompt.md >/dev/null; done` | serious | operator policy correction | done | reproduced: open set committed at `2f638d7f`; CDC reran the verify command during administrative close. | |
| F-2 | Parser build output installs executable `pandapi-parser` and does not install `pandaPIparser` or `pandaPIparser.legacy` in `dist/<platform>/`. | `set -e; make build-parser; . tools/shared/platform; platform="$(detect_platform)"; test -x "dist/$platform/pandapi-parser"; ! test -e "dist/$platform/pandaPIparser"; ! test -e "dist/$platform/pandaPIparser.legacy"` | serious | no inherited-name compatibility | deferred | reproduced blocker: `dist/macos-arm64/` contains `pandapi-parser`, `pandaPIparser`, and `pandaPIparser.legacy`; raw `pandaPIparser.legacy --status=stderr` exits 1 with `unrecognized option`. | Re-entry: Slice06 parser-native-contract-cutover. |
| F-3 | Grounder build output installs executable `pandapi-grounder` and does not install `pandaPIgrounder` or `pandaPIgrounder.legacy` in `dist/<platform>/`. | `set -e; make build-grounder; . tools/shared/platform; platform="$(detect_platform)"; test -x "dist/$platform/pandapi-grounder"; ! test -e "dist/$platform/pandaPIgrounder"; ! test -e "dist/$platform/pandaPIgrounder.legacy"` | serious | no inherited-name compatibility | deferred | reproduced blocker: `dist/macos-arm64/` contains `pandapi-grounder`, `pandaPIgrounder`, and `pandaPIgrounder.legacy`; raw `pandaPIgrounder.legacy --status=stderr` exits 1 with `unrecognized option`. | Re-entry: planned Slice07 grounder-native-contract-cutover. |
| F-4 | Transition shim scripts for parser and grounder are deleted, and no active build/test target invokes them. | `set -e; ! test -e scripts/pandapi-parser-adapter.sh; ! test -e scripts/pandapi-grounder-adapter.sh; ! test -e scripts/install-grounder-adapter.sh; ! rg -n -- "pandapi-(parser|grounder)-adapter|install-grounder-adapter" Makefile mk tests tools scripts README.md fixtures/contract` | serious | shim retirement | deferred | reproduced blocker: `scripts/pandapi-parser-adapter.sh`, `scripts/pandapi-grounder-adapter.sh`, and `scripts/install-grounder-adapter.sh` remain necessary for current managed behavior. | Re-entry: parser shim in Slice06, grounder shims in planned Slice07. |
| F-5 | Contract and smoke runners use canonical parser and grounder commands, while engine remains inherited until its owning slice. | `set -e; rg -n -- 'pandapi-parser|pandapi-grounder|pandaPIengine' tests/contract/run tests/smoke/run >/dev/null; ! rg -n -- 'pandaPIparser|pandaPIgrounder|\\.legacy|remains executable|legacy_binary=.*pandaPI(parser|grounder)|invoked_command=pandaPI(parser|grounder)' tests/contract/run tests/smoke/run` | serious | executable evidence surface | deferred | reproduced blocker: runners still target a transition state backed by sidecars and shims. | Re-entry: after native parser and grounder slices. |
| F-6 | Fixture records and fixture README no longer present inherited parser/grounder commands or raw legacy passthrough as accepted contract behavior. | `set -e; rg -n -- 'pandapi-parser|pandapi-grounder' fixtures/contract >/dev/null; ! rg -n -- 'pandaPIparser|pandaPIgrounder|\\.legacy|compatibility command|legacy passthrough|remains executable' fixtures/contract` | serious | contract documentation surface | deferred | reproduced blocker: fixture records describe the transition behavior that exists today. | Re-entry: parser records in Slice06, grounder records in planned Slice07. |
| F-7 | Make help, build/test targets, provenance check, macOS minimum-OS recording, and package helper metadata use canonical parser/grounder names. | `set -e; for term in pandapi-parser pandapi-grounder pandaPIengine; do rg -n -- "$term" mk tools Makefile >/dev/null; done; ! rg -n -- 'pandaPIparser|pandaPIgrounder|pandaPIparser\\.legacy|pandaPIgrounder\\.legacy' mk tools Makefile` | serious | build/tooling surface | deferred | reproduced blocker: `mk/build.mk` and `mk/tests.mk` still install/invoke parser and grounder transition shims. | Re-entry: parser tooling in Slice06, grounder tooling in planned Slice07. |
| F-8 | README and current active planning docs teach `pandapi-parser` and `pandapi-grounder`, and do not teach inherited parser/grounder commands as supported 0.3.0 usage. | `set -e; rg -n -- 'pandapi-parser|pandapi-grounder' README.md AGENTS.md docs/design-v0.3.0/project-plan.md docs/design-v0.3.0/arc05-binary-contract-adoption/arc-plan.md >/dev/null; ! rg -n -- 'pandaPIparser|pandaPIgrounder|pandaPI\\*.*remain|compatibility transition' README.md AGENTS.md docs/design-v0.3.0/project-plan.md docs/design-v0.3.0/arc05-binary-contract-adoption/arc-plan.md` | correctness-grade | user-facing surface | deferred | reproduced blocker: active README still teaches inherited parser/grounder commands. | Re-entry: parser-specific docs in Slice06, complete parser/grounder README cleanup after planned Slice07. |
| F-9 | Parser and grounder contract fixtures pass through make targets after the cutover. | `set -e; make test-contract-parser-managed; make test-contract-grounder-managed` | serious | contract preservation | deferred | not rerun as cutover proof because the cutover precondition failed; current passing managed fixtures depend on transition shims. | Re-entry: Slice06 for parser, planned Slice07 for grounder. |
| F-10 | Baseline, smoke, aggregate test, provenance, static/safety, format, actionlint, and macOS min-OS gates pass through `make check`. | `make check` | serious | release-branch quality gate | deferred | not rerun as cutover proof because the slice did not implement the cutover. | Re-entry: rerun after each native cutover slice. |
| F-11 | Release-package helper dry run, if locally runnable, packages canonical parser/grounder artifacts and does not package old parser/grounder names. | `set -e; make package-release TAG=v0.3.0-slice05-dryrun; if [ -d release/v0.3.0-slice05-dryrun ]; then ! rg -n -- 'pandaPIparser|pandaPIgrounder' release/v0.3.0-slice05-dryrun; rg -n -- 'pandapi-parser|pandapi-grounder|pandaPIengine' release/v0.3.0-slice05-dryrun >/dev/null; fi` | correctness-grade | package-helper alignment | deferred | not rerun as cutover proof because old parser/grounder artifacts still exist in `dist/`. | Re-entry: after native parser and grounder slices remove old artifacts. |
| F-12 | The implementation does not modify engine source, release publication workflow, checksums, final release manifests, or Arc05 close/CDC files. | `set -e; test -z "$({ git diff --name-only -- pandaPI/pandaPIengine .github/workflows/release.yml release-manifest.txt SHA256SUMS docs/design-v0.3.0/arc05-binary-contract-adoption/closing-report.md docs/design-v0.3.0/arc05-binary-contract-adoption/slice05-canonical-binary-cutover/cdc-verification.md; git diff --cached --name-only -- pandaPI/pandaPIengine .github/workflows/release.yml release-manifest.txt SHA256SUMS docs/design-v0.3.0/arc05-binary-contract-adoption/closing-report.md docs/design-v0.3.0/arc05-binary-contract-adoption/slice05-canonical-binary-cutover/cdc-verification.md; } | tr -d '\n')"` | serious | protected boundary | no-op | rationale: CC made no implementation edits after the open-set commit; CDC administrative close necessarily creates `cdc-verification.md`, which this row correctly forbade CC from creating. | Re-entry: preserve this boundary in implementation slices. |
| F-13 | The implementation adds no default-path network fetch, vendored package, held/rejected dependency import, or third-party API leakage into planner internals. | `set -e; ! { { git diff -U0 -- pandaPI pandapi-runtime fixtures mk tests tools scripts .github README.md; git diff --cached -U0 -- pandaPI pandapi-runtime fixtures mk tests tools scripts .github README.md; } | rg -n -e "FetchContent" -e "ExternalProject" -e "curl" -e "git clone" -e "find_package\\(nlohmann" -e "find_package\\(Boost" -e "find_package\\(absl" -e "find_package\\(GSL" -e "#include <nlohmann" -e "boost/" -e "absl/" -e "gsl/"; }` | serious | Arc02 dependency gates | no-op | rationale: no implementation diff landed for Slice05 after the open-set commit. | Re-entry: native parser/grounder slices must rerun dependency-gate checks. |
| F-14 | Staged and unstaged whitespace checks pass. | `set -e; git diff --check; git diff --cached --check` | serious | repository hygiene | done | reproduced: CDC ran `git diff --check` and `git diff --cached --check` during close. | |
| F-15 | CC writes a closing report with capability verdict, implementation decisions, verification, ledger walk, silent-drop check, and Bubble-up to Arc05. | `set -e; test -f docs/design-v0.3.0/arc05-binary-contract-adoption/slice05-canonical-binary-cutover/closing-report.md; for term in "Capability Verdict" "Implementation Decisions" Verification "Ledger Walk" "Silent-Drop Check" "Bubble-up to Arc05"; do rg -n -- "$term" docs/design-v0.3.0/arc05-binary-contract-adoption/slice05-canonical-binary-cutover/closing-report.md >/dev/null; done` | serious | ledger discipline | done | reproduced: CDC wrote an administrative closing report because CC hit the prompt's stop condition before implementation. | The close records non-delivery and deferral, not success. |

## Notes

- This slice supersedes earlier parser/grounder inherited-name compatibility
  expectations for active 0.3.0 surfaces. Historical closed reports should not
  be rewritten unless they are explicitly promoted into current instructions.
- "No shim" means no distributable parser/grounder artifact depends on
  `pandaPIparser`, `pandaPIgrounder`, or a `.legacy` sidecar to satisfy the
  0.3.0 managed-process contract.
- New GitHub Actions / CI jobs must invoke make targets, not project scripts.

## What Worked

- The prompt's stop condition prevented a dishonest shim deletion.
- The raw-binary probe exposed the missing prerequisite before churn began.
- Splitting the remediation by component keeps parser, grounder, and engine
  ownership one binary at a time.

## Closure

Closed administratively on 2026-08-10 after CC stopped at the explicit
amendment condition. Verified by: CDC. Rows: 15. Done: 3. Deferred: 10.
No-op: 2.
