# Arc06 Slice04: static-analysis-gate

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | Make exposes C++ static-analysis target(s), and `make help` lists the public entrypoint. | `set -e; make help | rg -n "static.*C\\+\\+|clang-tidy|analyzer|cpp-static|static-analysis-cpp"` | serious | Make entrypoint policy | proposed-done | `make help` lists `make static-analysis-cpp` under Quality. | Exact target name is implementation-owned, but it must be Make-backed and discoverable. |
| F-2 | The C++ static-analysis gate uses a reliable compile database for `pandaPI/runtime/`. | `set -e; make build-runtime; test -f build/runtime/$(uname -s | tr '[:upper:]' '[:lower:]')-$(uname -m)/compile_commands.json || test -f build/runtime/macos-arm64/compile_commands.json || test -f build/runtime/linux-x86_64/compile_commands.json; rg -n -- "compile_commands|pandaPI/runtime" Makefile mk pandaPI/runtime docs/design-v0.3.0/arc06-ci-and-test-hardening/slice04-static-analysis-gate >/dev/null` | correctness-grade | Slice03 coverage evidence; `ci-notes.md` | proposed-done | `make build-runtime` emits the normal runtime compile database, and `make static-analysis-cpp` configures a Debug runtime compile database under `build/static-analysis/runtime/macos-arm64/build/compile_commands.json`. | Use repo variables where possible rather than duplicating platform logic. |
| F-3 | The gate runs `clang-tidy` or explicitly records a missing-tool/platform deferral with a re-entry condition. | `set -e; rg -n -- "clang-tidy|missing tool|unavailable|Re-entry" Makefile mk pandaPI/runtime docs/design-v0.3.0/arc06-ci-and-test-hardening/slice04-static-analysis-gate >/dev/null` | serious | Arc06 A6 | proposed-done | `make static-analysis-cpp` ran `/opt/homebrew/opt/llvm/bin/clang-tidy` 20.1.6 over 14 runtime translation units and passed; the target also prints an explicit SKIP and re-entry condition if `clang-tidy` cannot be resolved. | Silent success without running or explicitly skipping is not acceptable. |
| F-4 | Clang Static Analyzer is either added where reproducible or explicitly deferred with reason and re-entry condition. | `set -e; rg -n -- "Clang Static Analyzer|scan-build|clang --analyze|defer|Re-entry" docs/design-v0.3.0/arc06-ci-and-test-hardening/slice04-static-analysis-gate pandaPI/runtime/README.md Makefile mk >/dev/null` | correctness-grade | Arc06 slice breakdown | proposed-done | The reproducible analyzer signal is `clang-tidy` with `clang-analyzer-*`; standalone `scan-build` is deferred with a re-entry condition because this slice does not need duplicate runtime build volume. | Do not force broad inherited build trees into analyzer scope. |
| F-5 | Included, excluded, and deferred paths are documented, with owned runtime code separate from inherited planner, generated, nested third-party, build, and dist paths. | `set -e; for term in "pandaPI/runtime" "inherited" "generated" "third-party" "build" "dist" "excluded" "deferred"; do rg -n -- "$term" docs/design-v0.3.0/arc06-ci-and-test-hardening/slice04-static-analysis-gate pandaPI/runtime/README.md >/dev/null; done` | correctness-grade | Arc06 ownership policy | proposed-done | Slice04 docs and `pandaPI/runtime/README.md` document included runtime source/tests/reached headers, excluded inherited/generated/third-party/build/dist paths, and deferred adoption seams. | This row prevents inherited warning volume from becoming an unclassified static-analysis blocker. |
| F-6 | Suppressions, if added, are narrow, justified, and documented; if none are needed, the no-suppression state is documented. | `set -e; rg -n -- "suppression|suppressions|NOLINT|no suppression|no-suppression" docs/design-v0.3.0/arc06-ci-and-test-hardening/slice04-static-analysis-gate pandaPI/runtime Makefile mk >/dev/null` | correctness-grade | static-analysis ownership | proposed-done | No suppressions or `NOLINT` annotations were added; docs require future suppressions to be narrow, check-specific, and justified. | Broad unowned suppressions are not acceptable. |
| F-7 | Adoption-seam static analysis for `pandapi_parser_native.cpp`, `pandapi_grounder_native.cpp`, and `pandapi_engine_native.cpp` is either included through reliable compile commands or deferred with a re-entry condition. | `set -e; rg -n -- "pandapi_parser_native|pandapi_grounder_native|pandapi_engine_native|adoption seam|Re-entry" docs/design-v0.3.0/arc06-ci-and-test-hardening/slice04-static-analysis-gate docs/design-v0.3.0/arc06-ci-and-test-hardening/fixture-gap-inventory.md >/dev/null` | correctness-grade | Arc06 A6; Slice03 bubble-up | proposed-deferred | Adoption-seam static analysis is deferred because current compile commands for the native files are produced through component-specific inherited build trees and copied source directories. | Re-entry: include `pandapi_parser_native.cpp`, `pandapi_grounder_native.cpp`, and `pandapi_engine_native.cpp` after canonical `pandapi-*` builds expose reliable owned-file compile commands while inherited/generated and third-party paths remain excluded or separately classified. |
| F-8 | Any new CI or workflow behavior invokes Make targets rather than direct tool or harness commands. | `make safety-checks && make actionlint` | serious | CI entrypoint policy | proposed-done | No workflow was changed; `make safety-checks && make actionlint` passed. | If no workflow changes are made, this still proves the safety check. |
| F-9 | Existing quality and behavior gates still pass after static-analysis wiring. | `make static-analysis && make format-check && make test && make provenance-check && git diff --check && git diff --cached --check` | serious | no regression | proposed-done | `make static-analysis`, `make format-check`, `make test`, `make provenance-check`, `git diff --check`, and `git diff --cached --check` pass after static-analysis wiring. | `make check` may be used instead if it covers these gates on the local platform. |
| F-10 | The slice stays inside Arc06 static-analysis scope and does not modify parser/grounder/engine product behavior, release packaging/publication, README migration prose, Arc07, or Arc08 surfaces. | `set -e; ! git diff --cached --name-only -- pandaPI/parser pandaPI/grounder pandaPI/engine README.md release tools/release .github/workflows/release.yml docs/design-v0.3.0/arc07-pandapi-tutorial-docs docs/design-v0.3.0/arc08-release-prep-publication | rg .` | serious | Arc06 boundary | proposed-done | Staged boundary check passed; touched files stay in Make static-analysis wiring, `pandaPI/runtime/README.md`, and Arc06 Slice04/inventory docs. | Runtime docs/source and Make static-analysis wiring are allowed. |

## What Good Looks Like

- The static-analysis target is easy to find from `make help`.
- The first meaningful signal is owned runtime/process-policy code, not a wall
  of unclassified inherited planner findings.
- Missing tools and deferred analyzer/adoption-seam scopes are named with
  concrete re-entry conditions.
