# Slice 03: diagnostics-status-io

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | The slice open set documents diagnostics/status I/O scope, Arc03 tagged status requirements, no-binary-adoption boundary, Arc02 fmt gate, and C++ Core Guidelines pressure. | `set -e; test -f docs/design-v0.3.0/arc04-shared-runtime-substrate/slice03-diagnostics-status-io/slice-doc.md; for term in diagnostics "PANDAPI_STATUS" stdout stderr flushing partial-output "No edits under" fmt "Core Guidelines" I.4 E.1 E.27 "behavior is not changed"; do rg -n "$term" docs/design-v0.3.0/arc04-shared-runtime-substrate/slice03-diagnostics-status-io/slice-doc.md >/dev/null; done` | serious | Arc04 slice03 planning | done | Slice doc open set retained required terms; command passed during CC close. | |
| F-2 | `pandapi-runtime/README.md` is updated for the implemented diagnostics/status I/O facade and no longer contains stale skeleton wording that says the helpers do not exist. | `set -e; for term in "Diagnostics/status I/O" "PANDAPI_STATUS" "stdout/stderr ownership" "partial-output" "no binary adoption" "standard-library-only"; do rg -n "$term" pandapi-runtime/README.md >/dev/null; done; ! rg -n -e "Those helpers do not exist in this skeleton yet" -e "Slice01 only establishes the buildable runtime boundary" pandapi-runtime/README.md` | serious | Arc04 slice03 documentation | done | README documents implemented status I/O facade and stale skeleton wording is removed. | |
| F-3 | A public status I/O header exists under `pandapi-runtime/include/pandapi/runtime/` and defines typed status record, status stream, and partial-output policy vocabulary. | `set -e; test -f pandapi-runtime/include/pandapi/runtime/status_io.hpp; for term in "namespace pandapi::runtime" StatusRecord StatusStream PartialOutputPolicy ProcessStatus StatusResult; do rg -n "$term" pandapi-runtime/include/pandapi/runtime/status_io.hpp >/dev/null; done` | correctness-grade | Arc03 status I/O contract; C++ Core Guidelines I.1/I.4 | done | `status_io.hpp` defines `StatusRecord`, `StatusStream`, and `PartialOutputPolicy` using Slice02 status/result types. | |
| F-4 | Runtime APIs expose status stream legality helpers that encode Arc03 stdout/stderr ownership. | `set -e; for term in StatusStream Stderr Stdout OutputRole DataArtifact Informational TaggedStatus status_stream "stdout" "stderr"; do rg -n "$term" pandapi-runtime/include/pandapi/runtime/status_io.hpp pandapi-runtime/src pandapi-runtime/tests >/dev/null; done` | correctness-grade | Arc03 stdout/stderr ownership | done | `status_stream_allowed` permits stderr always and stdout only for empty/tagged-status ownership; tests cover data and informational rejection. | |
| F-5 | The tagged status writer emits a single-line `PANDAPI_STATUS` record with required fields `status`, `component`, `surface`, `surface_disposition`, `exit_code`, and `class`. | `set -e; for term in "PANDAPI_STATUS" "status=" "component=" "surface=" "surface_disposition=" "exit_code=" "class="; do rg -n "$term" pandapi-runtime/include/pandapi/runtime/status_io.hpp pandapi-runtime/src pandapi-runtime/tests >/dev/null; done` | correctness-grade | Arc03 tagged text status | done | `serialize_status_record` emits the required tagged text fields; tests assert the exact success line. | |
| F-6 | The tagged status parser accepts valid status records and returns a local value-or-status result without requiring diagnostic prose parsing. | `set -e; for term in parse StatusResult StatusRecord "PANDAPI_STATUS" "has_value" "input_invalid"; do rg -n "$term" pandapi-runtime/include/pandapi/runtime/status_io.hpp pandapi-runtime/src pandapi-runtime/tests >/dev/null; done` | correctness-grade | Arc03 machine observation; Slice02 result facade | done | `parse_status_record` returns `StatusResult<StatusRecord>` and maps malformed machine text to `input_invalid`; iteration01 makes numeric parsing overflow-safe with oversized `exit_code` and `signal_number` regressions. | |
| F-7 | Seam tests prove writer/parser round trips for at least one success status and one non-success status. | `set -e; for term in "ok" "domain_no_plan" "PANDAPI_STATUS" "round"; do rg -n "$term" pandapi-runtime/tests >/dev/null; done` | serious | Arc03 status vocabulary adoption | done | `status_io_smoke.cpp` round-trips `ok` and `domain_no_plan`. | |
| F-8 | Emitted field values are single-line and either escape or reject malformed values containing newline, tab, ANSI escape, or placeholder prose. | `set -e; for term in newline tab ANSI escape malformed placeholder "single-line"; do rg -n "$term" pandapi-runtime/src pandapi-runtime/tests docs/design-v0.3.0/arc04-shared-runtime-substrate/slice03-diagnostics-status-io/closing-report.md >/dev/null; done` | correctness-grade | Arc03 status safety requirement | done | Escaping covers newline and tab; ANSI and placeholder prose are rejected; tests assert no raw embedded newline. | |
| F-9 | Final status writing flushes the selected stream before returning from the facade. | `rg -n -e flush -e "std::flush" pandapi-runtime/include/pandapi/runtime/status_io.hpp pandapi-runtime/src pandapi-runtime/tests` | correctness-grade | Arc03 final-status flushing | done | `write_status_record` calls `flush()` and `status_io_smoke.cpp` verifies one flush through a custom stream buffer. | |
| F-10 | Partial-output policy vocabulary covers absent, retained, discarded, complete, and unknown outcomes. | `set -e; for term in PartialOutputPolicy Absent Retained Discarded Complete Unknown partial_output_policy; do rg -n "$term" pandapi-runtime/include/pandapi/runtime/status_io.hpp pandapi-runtime/src pandapi-runtime/tests pandapi-runtime/README.md >/dev/null; done` | serious | Arc03 partial-output policy | done | `PartialOutputPolicy` maps absent, retained, discarded, complete, and unknown; parser/writer tests cover the field. | |
| F-11 | The status I/O implementation is C++17 standard-library-only and does not vendor, fetch, discover, include, or expose fmt, CLI11, Catch2, `tl::expected`, reproc++, nlohmann/json, Boost, Abseil, or GSL. | `! rg -n -e "FetchContent" -e "ExternalProject" -e "find_package\\(fmt" -e "find_package\\(CLI11" -e "find_package\\(Catch2" -e "find_package\\(tl" -e "find_package\\(reproc" -e "find_package\\(nlohmann" -e "find_package\\(Boost" -e "find_package\\(absl" -e "find_package\\(GSL" -e "#include <fmt" -e "#include \\\"CLI11" -e "#include <catch2" -e "expected.hpp" -e "tl/expected" -e "reproc" -e "nlohmann" -e "boost/" -e "absl/" -e "gsl/" pandapi-runtime/CMakeLists.txt pandapi-runtime/include pandapi-runtime/src pandapi-runtime/tests scripts/build-runtime.sh` | serious | Arc02 dependency gates | done | No external dependency fetch, discovery, include, or exposed third-party type was added. | |
| F-12 | New public headers avoid global `using namespace`, mutable namespace-scope state, non-inline object definitions, raw ownership transfer, and explicit allocation/deallocation. | `! rg -n -e "using namespace" -e "extern [^;]*=" -e "^[A-Za-z_:<>]+ \\*" -e "new " -e "delete " -e "malloc\\(" -e "free\\(" pandapi-runtime/include/pandapi/runtime` | correctness-grade | C++ Core Guidelines I.2/I.11/SF.2/SF.7/R.11 | done | Public headers remain declarations and typed values only, with no ownership-transfer or global-state patterns. | |
| F-13 | Mapping or validation helpers that cannot fail are declared or defined `noexcept`, and tests exercise status I/O through public headers. | `set -e; rg -n "noexcept" pandapi-runtime/include/pandapi/runtime/status_io.hpp pandapi-runtime/src pandapi-runtime/tests >/dev/null; rg -n "status_io.hpp" pandapi-runtime/tests >/dev/null` | correctness-grade | C++ Core Guidelines F.6/E.12 | done | Non-failing stream/policy/accessor helpers are `noexcept`; seam test includes the public header. | |
| F-14 | `pandapi-runtime/CMakeLists.txt` builds the new status I/O source and registers a status I/O seam test. | `set -e; for term in status_io add_executable target_link_libraries add_test; do rg -n "$term" pandapi-runtime/CMakeLists.txt >/dev/null; done` | serious | Arc04 runtime test substrate | done | CMake builds `src/status_io.cpp` and registers `pandapi_status_io_smoke`. | |
| F-15 | `./scripts/build-runtime.sh` builds the runtime and runs all registered runtime tests successfully. | `./scripts/build-runtime.sh` | serious | executable substrate proof | done | Passed on macos-arm64 after iteration01; CTest reported 3/3 tests passed, including overflow-safe numeric parser regressions. | |
| F-16 | Existing parser, grounder, and engine builds still pass after diagnostics/status I/O lands. | `./scripts/build-parser.sh && ./scripts/build-grounder.sh && ./scripts/build-engine.sh` | serious | Arc04 no behavior drift | done | Parser, grounder, and engine builds passed on macos-arm64 after iteration01; inherited warnings only. | |
| F-17 | Existing negative smoke behavior still passes after diagnostics/status I/O lands. | `./scripts/smoke-test.sh --negative` | serious | Arc04 no behavior drift | done | Negative smoke passed after iteration01: 4 passed, 0 failed. | |
| F-18 | The slice does not edit inherited planner source, workflows, top-level README, release assets, manifest, historical pins, `dist`, `build`, or `release` paths. | `git diff --name-only -- pandaPI .github README.md release-manifest.txt vendor.env pins.env dist build release` | serious | Arc04 no-behavior/release-shape boundary | done | Protected path diff check produced no output. | |
| F-19 | CC writes a closing report with a capability verdict, implementation decisions, verification, per-row ledger walk, silent-drop check, and Bubble-up to Arc04. | `set -e; test -f docs/design-v0.3.0/arc04-shared-runtime-substrate/slice03-diagnostics-status-io/closing-report.md; for term in "Capability Verdict" "Implementation Decisions" Verification "Ledger Walk" "Silent-Drop Check" "Bubble-up to Arc04"; do rg -n "$term" docs/design-v0.3.0/arc04-shared-runtime-substrate/slice03-diagnostics-status-io/closing-report.md >/dev/null; done` | serious | ledger discipline | done | Closing report added with required sections and all row outcomes. | |
| F-20 | CC does not create CDC's independent verification file. | `! test -e docs/design-v0.3.0/arc04-shared-runtime-substrate/slice03-diagnostics-status-io/cdc-verification.md` | serious | CDC/CC role separation | done | No CDC verification file exists in the slice directory. | |

## What Worked

To be completed by CC at close.

## Closure

Closed by CC on 2026-08-09. CDC verification: pending. Total rows: 20. Done:
20. Deferred: 0. No-op: 0.
