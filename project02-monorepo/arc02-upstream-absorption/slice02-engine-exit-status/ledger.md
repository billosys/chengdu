# Slice 02: engine-exit-status

> Ledger per `LEDGER-DISCIPLINE.md` (v2.0), Section A. All rows open at
> slice start, 2026-08-08. Closer: CC. Verifier: CDC. This slice absorbs
> upstream pandaPIengine PR #14 / issue #12 as the first source-changing
> Arc02 slice.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | Slice starts from `release/0.2.x` with Slice01 closed, CDC-verified, and the worktree clean. | `git branch --show-current`; `git status --short --branch`; `test -f docs/design-v0.2.0/arc02-upstream-absorption/slice01-upstream-triage/closing-report.md`; `test -f docs/design-v0.2.0/arc02-upstream-absorption/slice01-upstream-triage/cdc-verification.md` | serious | slice-doc section 1 | done | attested: branch `release/0.2.x`; start status `## release/0.2.x...origin/release/0.2.x [ahead 2]`; Slice01 `closing-report.md` and `cdc-verification.md` present. | Ahead count was expected local slice-open history. No unrelated tracked work was present at slice start. |
| F-2 | Upstream PR #14 and issue #12 are refreshed at implementation time, with state, head SHA, author identity, merge/conflict status, and issue return-code intent recorded. | `gh api repos/panda-planner-dev/pandaPIengine/pulls/14`; `gh api repos/panda-planner-dev/pandaPIengine/pulls/14/commits`; `gh api repos/panda-planner-dev/pandaPIengine/issues/12`; `gh api repos/panda-planner-dev/pandaPIengine/issues/12/comments` | serious | arc-plan section 2 | done | attested: PR #14 still open/non-draft, head `304048392e2b3ee53aca7f93d88b5a35230f638b`, base `17e8d98cbbaed38ee25b136e8ead08332c673275`, mergeable false / state `dirty`, 4 commits, 7 files, +387/-376. All PR commits authored by Robert P. Goldman `<rpgoldman@sift.net>`. Issue #12 still open; comments preserve exit `2` intent and SAT 10/20 ordinary-state guidance. | Refreshed on 2026-08-08 before source edits. |
| F-3 | The PR #14 absorption method is disclosed: clean cherry-pick where possible, bounded manual port where conflicts require it, with no generated `.gitignore` or formatting-only churn. | `git apply --check --directory=pandaPI/pandaPIengine <pr14.diff>` or `git cherry-pick -Xsubtree=... --no-commit` in reversible state; `git diff --stat`; `git diff --check` | correctness | project-plan section 3 absorption with credit | done | attested: `gh pr diff 14 --repo panda-planner-dev/pandaPIengine --patch > /tmp/chengdu-pr14.patch`; `git apply --check --directory=pandaPI/pandaPIengine /tmp/chengdu-pr14.patch` failed in `SearchEngine.cpp`, `sat_planner.cpp`, and `sat_planner.h`. Manual port commit `4da38819` changed 7 files, +28/-23; `git diff --check` passed after indentation cleanup. | PR formatting churn and generated `.gitignore` additions were not ported. |
| F-4 | Engine process exit status now propagates search outcome: solved/success exits `0`; search failure / proven-unsolvable exits `2`; errors remain nonzero and are not reported as success. | source inspection of `SearchEngine.cpp` and SAT planner return plumbing; direct positive and unsolvable engine invocations via smoke tests; grep for final `return` paths | serious | upstream issue #12; PR #14 | done | attested: `SearchEngine.cpp` initializes `searchRes`, assigns progression/SAT/translation results, sets unavailable SAT/BDD support to `1`, and returns `searchRes`; `PriorityQueueSearch::search` returns `0` for solved and default `2` for timeout/proven-unsolvable. Positive smoke passed solve `exit 0`; negative smoke passed unsolvable `exit 2` plus `Status: Proven unsolvable`. | Interactive and BDD compiled paths remain outside this bounded redesign. |
| F-5 | SAT planner return plumbing is made explicit enough to compile and preserve issue #12's maintainer guidance around SAT solver return states. | inspect `pandaPI/pandaPIengine/src/sat/sat_planner.cpp` and `.h`; build engine; targeted grep for `solve_with_sat_planner` return type and return statements | correctness | issue #12 comments; PR #14 files | done | attested: `sat_planner.h` declares `int solve_with_sat_planner(...)`; `sat_planner.cpp` returns `0` when a SAT solution is printed and keeps non-10/20 SAT solver states on the existing `exit(2)` error path. `./scripts/build-engine.sh` passed on macos-arm64. | Normal release smoke does not enable SAT, so this row is compile/source-inspection coverage. |
| F-6 | Negative smoke updates the unsolvable gate to expect engine exit `2` plus `Status: Proven unsolvable`, classified as `UNSOLVABLE`, not success and not generic failure. | `./scripts/smoke-test.sh --negative`; inspect relevant script lines and PASS text | serious | slice-doc section 3 | done | attested: `scripts/smoke-test.sh` header and gate now say engine exit `2`; `./scripts/smoke-test.sh --negative` -> 4 passed, 0 failed, including `negative: unsolvable -> engine exit 2, Status: Proven unsolvable (UNSOLVABLE, not success, not failure)`. | Bash 3.2-compatible change only. |
| F-7 | Positive smoke still proves the minimal fixture end-to-end: parse, ground, solve exit `0` with `Status: Solved`, convert, verify true. | `./scripts/smoke-test.sh`; inspect summary and solve gate output | serious | project DoD; regression guard | done | attested: `./scripts/smoke-test.sh` -> 5 passed, 0 failed on macos-arm64; solve gate reported `exit 0, Status: Solved`; verify gate reported `Plan verification result: true`. | Confirms the change does not make all engine exits nonzero. |
| F-8 | Full local no-fetch source gate passes after the source change. | from clean generated output where practical: `./scripts/build-parser.sh`; `./scripts/build-grounder.sh`; `./scripts/build-engine.sh`; `./scripts/check-provenance.sh`; positive smoke; negative smoke | serious | arc-plan A4/A5 | done | attested: parser, grounder, and engine builds passed on macos-arm64. Initial provenance check exposed stale ignored `dist/macos-arm64/provenance.txt` blocks from earlier builds; after clearing the ignored generated file and rebuilding all three artifacts, `./scripts/check-provenance.sh` passed. Positive and negative smoke passed. | The stale-provenance incident was generated-output hygiene, not a tracked-source change. |
| F-9 | Package dry-run preserves wolong's frozen release shape and validates checksums/manifests after the source change. | `./scripts/package-release.sh v0.2.0-slice02-smoke`; inspect `release/`; checksum verification used by the package script | serious | project ledger P5 precursor | done | attested: `./scripts/package-release.sh v0.2.0-slice02-smoke` passed for macos-arm64; built `pandapi-v0.2.0-slice02-smoke-macos-arm64.tar.gz`, `THIRD-PARTY-LICENSES`, `release-manifest.txt`, `SHA256SUMS`, and `notes.md`. | No release was published. |
| F-10 | Maintained shell/workflow/static checks pass and docs are diff-clean. | `bash -n scripts/*.sh`; `/bin/bash -n scripts/*.sh` where available; `shellcheck scripts/*.sh`; `actionlint .github/workflows/*.yml`; `git diff --check` | polish | local quality gate | done | attested: `bash -n scripts/*.sh`, `/bin/bash -n scripts/*.sh`, `shellcheck scripts/*.sh`, and `actionlint .github/workflows/*.yml` all exited 0. First `git diff --check` found three indentation issues; after cleanup, `git diff --check` exited 0. | All requested tools were available locally. |
| F-11 | The committed source/doc diff stays inside slice scope: expected engine files, smoke/docs updates, ledger/closing report; no parser PR #21 or 0.3.0 redesign changes. | `git diff --name-only <slice-start>..HEAD`; `git diff --stat <slice-start>..HEAD`; targeted grep for unrelated touched paths/features | serious | slice-doc scope/out | done | attested: implementation commit `4da38819` touched only `pandaPI/pandaPIengine/src/SearchEngine.cpp`, SAT planner `.cpp/.h`, `search/PriorityQueueSearch.h`, translation controller `.cpp/.h`, and `scripts/smoke-test.sh`; docs-close changes are limited to this slice ledger/report. | No parser, grounder, release-shape, wolong API, or 0.3.0 redesign changes. |
| F-12 | Upstream credit is present in implementation commits: original author identity preserved where cherry-picked, or Robert P. Goldman explicitly credited for a manual port; upstream PR and issue URLs are in commit trailers/body. | `git log --format=fuller <slice-start>..HEAD -- pandaPI/pandaPIengine`; inspect commit trailers/body | serious | project ledger P3; arc ledger A2 | done | attested: implementation commit `4da38819ae0d556a9a8c22972c1954e98b846c03` includes `Upstream-author: Robert P. Goldman <rpgoldman@sift.net>`, `Upstream-PR: https://github.com/panda-planner-dev/pandaPIengine/pull/14`, `Upstream-Issue: https://github.com/panda-planner-dev/pandaPIengine/issues/12`, and both project co-author trailers. | Native author preservation was not possible because the upstream diff did not apply cleanly. |
| F-13 | Repo hygiene holds at close: worktree clean, no generated outputs under `pandaPI/`, no temporary PR worktree/branch/remotes left behind. | `git status --short --branch`; `find pandaPI -name .git -print`; `git ls-files -s pandaPI | awk '$1 == "160000"'`; `git worktree list`; `git remote -v` | correctness | arc01 no-gitlink/no-generated baseline | done | attested before docs-close commit: `find pandaPI -name .git -print` empty; `git ls-files -s pandaPI | awk '$1 == "160000"'` empty; `git worktree list` only `/Users/oubiwann/lab/billosys/chengdu`; `git remote -v` only `origin`. Tracked worktree contained only intended docs-close files while closing. | Ignored `dist/`, `build/`, `release/`, and `upstream/` outputs are not tracked. |
| F-14 | Slice close report walks all 14 rows and includes Bubble-up to the arc/project: delivered-as-assigned, any semantic surprise, whether arc/project docs changed, and silent-drop diff. | `test -f docs/design-v0.2.0/arc02-upstream-absorption/slice02-engine-exit-status/closing-report.md`; inspect row count and Bubble-up section | serious | PROJECT-MANAGEMENT Part IV | done | attested: `closing-report.md` written with row-by-row closure and Bubble-up. | No `cdc-verification.md` was written by CC. |

## What Worked

Manual port stayed small because the local in-tree engine already diverged
enough from PR #14 that applying the patch would have carried conflicts and
formatting churn. The smoke harness made the behavior change concrete:
positive solved search stayed `0`, and unsolvable search moved to `2` with the
same `Status: Proven unsolvable` line.

## Follow-ups / Deferrals

CDC should rerun the full gate from a clean generated output directory. The
stale ignored provenance blocks found during CC are a useful reminder that
`dist/<platform>/provenance.txt` should be generated from a clean file before
provenance/package checks.
