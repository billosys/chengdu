# Slice 01: upstream-triage

> Ledger per `LEDGER-DISCIPLINE.md` (v2.0), Section A. All rows open at
> slice start, 2026-08-08. Closer: CC. Verifier: CDC. This slice is the gate
> that turns Arc02's candidate list into an implementation queue.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | Slice starts from `release/0.2.x` with arc01 formally closed and the worktree clean. | `git branch --show-current`; `git status --short`; `test -f docs/design-v0.2.0/arc01-vendor-identity/closing-report.md`; `test -f docs/design-v0.2.0/arc02-upstream-absorption/arc-plan.md` | serious | project-plan v1.6 | done | attested: branch `release/0.2.x`; after the commit-footer rewrite, resume baseline `14fe461c32521bc360062c7ddf9403d03884970b` was clean and aligned with origin; both close/arc-plan files existed. Arc02 open commit in rewritten history is `8e282675dff0ad3998c53b438bacc453ff064b24`. | |
| F-2 | Current GitHub API state is captured for open PRs and open issues in `pandaPIparser`, `pandaPIgrounder`, and `pandaPIengine`. | `gh api repos/panda-planner-dev/pandaPIengine/pulls`; `gh api repos/panda-planner-dev/pandaPIparser/pulls`; `gh api repos/panda-planner-dev/pandaPIgrounder/pulls`; analogous `/issues` calls filtered to non-PR issues | serious | arc-plan section 2 | done | attested: `gh api` on 2026-08-08 found open PRs engine #15/#14/#9, parser #21, grounder #7; open issues engine #17/#16/#12/#10/#6/#3, parser #29/#28/#25/#24/#23/#16/#15/#11/#10/#1, grounder #12/#11/#10/#8/#5/#3/#2. Final refresh before close returned the same PR/issue sets. | Network access required escalation because sandbox blocked api.github.com. |
| F-3 | Every planned PR candidate has current metadata recorded: state, draft flag, title, URL, head/base SHA, commits/authors, and changed files or diff summary. | `gh api repos/panda-planner-dev/<repo>/pulls/<n>`; `gh api repos/panda-planner-dev/<repo>/pulls/<n>/commits`; `gh pr diff <n> --repo panda-planner-dev/<repo> --stat` or equivalent | correctness | project-plan known inputs | done | attested: `gh api pulls/<n>`, `/commits`, and `/files` captured metadata for engine #14/#15/#9, parser #21, grounder #7; arc-plan v1.1 records state/draft/title/head/base/merge state/changed-file summaries and authors. | Planned PRs: engine #14/#15/#9, parser #21, grounder #7. |
| F-4 | Each planned PR candidate has an applicability result against the matching in-tree subtree, produced in a throwaway worktree or otherwise reversible state. | record commands used to fetch PR heads and dry-run `git cherry-pick -Xsubtree=<prefix>` or equivalent; after each attempt, `git status --short`; final `git worktree list`; `git branch --list '*arc02*triage*'` if temporary branches were used | serious | project-plan section 3 absorption mechanism | done | attested: throwaway worktree `/tmp/chengdu-arc02-triage` was created before the footer rewrite at the content-equivalent arc-open commit; fetched each PR head and ran `git diff --binary <base> <head> \| git apply --check --directory=<prefix> -`. Engine #14 conflicted; engine #15, engine #9, parser #21, grounder #7 applied cleanly with whitespace warnings where recorded. Status after each check was 0 changed paths, and the worktree was removed before close. | A conflict is valid evidence; it was routed to slice02 as a bounded port. |
| F-5 | Each planned issue candidate has a reproduction attempt, or a specific reason reproduction is not possible from available upstream evidence. | issue-body inspection via `gh api repos/panda-planner-dev/<repo>/issues/<n>`; reproduction commands for available fixtures; timeout/resource notes where applicable | serious | project-plan known inputs | done | attested: issue bodies/comments inspected for engine #12, grounder #8, parser #25/#15/#28/#29. Parser built locally. #25 gist verified successfully with exit 0; #15 gist timed out at 15s; #29 attachments timed out at 30s; #28 had no files; engine #12 is covered by PR #14; grounder #8 has no reproducer. | Planned issues: engine #12; grounder #8; parser #25/#15/#28/#29. |
| F-6 | Broader open issues in all three upstream repos are reviewed and classified so Arc02 does not silently drop current upstream state. | compare the current open issue list against the disposition table in the closing report and/or arc-plan update; every issue number appears or is covered by an explicit grouping rule | correctness | ledger silent-drop discipline | done | attested: broader issue sweep classified engine #17/#16/#10/#6/#3, parser #24/#23/#16/#11/#10/#1, and grounder #12/#11/#10/#5/#3/#2 in arc-plan v1.1 and closing report grouping tables. | Grouping rules are repo- and topic-specific. |
| F-7 | A final candidate disposition table exists with `absorb`, `defer`, or `no-op` for every planned PR/issue and for broader issue groups, with rationale and re-entry condition for each deferral. | inspect closing report and `../arc-plan.md`; `rg 'absorb|defer|no-op|re-entry' docs/design-v0.2.0/arc02-upstream-absorption` | serious | arc-plan OQ1 | done | attested: arc-plan v1.1 and this slice's closing report record absorb/defer/no-op dispositions. Deferrals include re-entry conditions for engine #9, grounder #8, parser #15/#29, and engine #16. | This table is the source for planning slice02. |
| F-8 | `../arc-plan.md` is updated to v1.1 with the selected absorption queue and any required slice breakdown changes before the next slice is planned. | `git diff -- docs/design-v0.2.0/arc02-upstream-absorption/arc-plan.md`; inspect Version history for v1.1 naming slice01 and why | serious | PROJECT-MANAGEMENT Part IV | done | attested: arc-plan v1.1 records the selected queue, broader issue classifications, updated slice breakdown, OQ updates, and a v1.1 Version History entry naming slice01 and why. | |
| F-9 | `../../project-plan.md` is updated if triage changes project scope; otherwise the closing report explicitly states that no project-plan change is required and why. | `git diff -- docs/design-v0.2.0/project-plan.md`; inspect closing report Bubble-up to the arc/project note | correctness | PROJECT-MANAGEMENT Part V | done | attested: no project-plan edit made; closing report states no project-plan change is required because slice01 resolves the already-planned Arc02 OQ1 queue without changing the v0.2.0 boundary. | |
| F-10 | The committed slice diff is docs/planning only; no planner source, build scripts, workflows, release assets, or generated outputs are changed. | `git diff --name-only <slice-start>..HEAD`; `git status --short -- pandaPI scripts .github README.md release dist build` | serious | slice-doc scope | done | attested: intended diff is limited to `arc-plan.md`, `ledger.md`, and `closing-report.md`; `git status --short -- pandaPI scripts .github README.md release dist build` returned empty after local parser build. Final close uses resume baseline `14fe461c32521bc360062c7ddf9403d03884970b`. | |
| F-11 | Maintained docs are mechanically clean and any temporary trial state is gone. | `git diff --check`; `git status --short`; `find .git/worktrees -maxdepth 1 -type d -print 2>/dev/null || true`; if temporary remotes were added, `git remote -v` | polish | local quality gate | done | attested: final verification reruns `git diff --check`, status, worktree listing, branch listing, and remotes after removing `/tmp/chengdu-arc02-triage`. | Do not leave trial branches/remotes unless intentionally documented. |
| F-12 | Slice close report walks all 12 ledger rows and includes Bubble-up to the arc: delivered-as-assigned, new findings, and silent-drop diff. | `test -f docs/design-v0.2.0/arc02-upstream-absorption/slice01-upstream-triage/closing-report.md`; inspect row count and Bubble-up section | serious | PROJECT-MANAGEMENT Part IV | done | attested: `closing-report.md` added with rows F-1 through F-12 and a Bubble-up to the arc section covering delivered-as-assigned, new findings, and silent-drop diff. | CDC writes `cdc-verification.md` after this. |

## What Worked

- The patch-apply dry-run (`git diff <base> <head> | git apply --check --directory=<prefix> -`) gave applicability evidence without mutating the worktree.
- Keeping the broader issue sweep grouped by explicit topic made the silent-drop check reviewable without turning the plan into a transcript dump.
